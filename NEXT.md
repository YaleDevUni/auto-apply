## 상주 브라우저를 공유하기 시작하면서 생긴 사고 3종 (2026-08-16)

창 누수를 고치려고 "한 번 띄우고 계속 붙는다"로 바꾼 뒤(d1848d6), 브라우저가
**공유 자원**이 됐다. 그런데 이 저장소는 긴 작업을 전부 `subprocess.Popen`으로
fire-and-forget 한다 — 서로의 존재를 아무도 모르는 채 하나의 창을 나눠 쓰는
구조가 됐고, 아래 셋이 한꺼번에 터졌다. **공유 자원으로 바꿀 때는 "누가
쓰는가"와 "누가 소유하는가"를 같이 정해야 한다**는 게 교훈이다.

**1. 포트는 신원이 아니다.** `_attach()`가 CDP 9222가 응답하면 그냥 붙었다.
9222는 흔한 기본값이라 다른 크롬이 얼마든 같은 포트를 잡는다 — 사람이 클로드
재로그인(OAuth)하려고 띄운 크롬에서 우리 작업이 돌았다. 더 나빴던 건
`_kill_resident()`로, 포트 점유자를 무조건 kill 했다(사람 브라우저를 죽이는
코드다). 이제 lsof로 pid를 찾아 `ps`의 명령줄에서 `--user-data-dir=<BROWSER_DIR>`을
확인한다(`resident_owner()`). 프로필이 곧 로그인 세션이 사는 곳이라, 프로필이
같다는 건 "우리 세션이 있는 창"과 같은 말이다.

**2. `pages[0]`은 우리 탭이 아니다.** 그냥 "첫 번째 탭"이다. 사람이 그 창에
탭 하나를 열면 자동화가 그걸 몰고 다닌다. 만든 탭의 CDP targetId를
`.browser_tab`에 적어두고 그 탭만 쓴다. URL·제목으로 고르면 안 된다 — 사람이
원티드를 직접 열어두는 일이 잦다. (실측: 우리 탭을 닫고 사람 탭만 남긴
상태에서 붙여보면, 예전 코드가 사람 탭을 몰았을 자리에서 새 탭을 연다.)

**3. 뒤에 온 작업이 앞의 탭을 뺏는다.** 수집 중에 폰에서 지원 버튼을 누르면
뒤에 온 쪽이 같은 탭을 다른 URL로 몰고 갔고, 앞 작업은 셀렉터를 기다리다
타임아웃으로 죽었다. 남는 메시지가 "요소를 못 찾음"이라 **진짜 원인이 어디에도
안 남는다** — 이 종류의 실패는 로그만 봐서는 영원히 못 찾는다. flock 파일
잠금으로 직렬화한다(`runner/lock.py`). 파이썬 안의 락으로는 안 된다 —
경쟁하는 쪽이 스레드가 아니라 별개 프로세스다.

### 곁가지로 드러난 것: 부분 수집이 본문을 지웠다

중단 기능을 검증하다 발견했다. 저장 루프는 상세를 받았든 못 받았든 전체를
훑는데, 못 받은 공고는 `description=''`로 와서 **예전에 받아둔 본문을
덮어썼다.** 22건에서 멈춘 수집 한 번에 actionable이 16 → 3으로 주저앉았다.
`detail_limit`(800)을 넘긴 공고에는 늘 일어나던 일이고, 중단 기능이 그걸 크게
만들어 드러냈을 뿐이다. `upsert_job`이 빈 본문으로는 덮어쓰지 않게 고쳤다.

같은 이유로 **중단된 수집으로는 건강을 판정하지 않는다.** 상세를 덜 받았으니
NO_DETAIL이 통과분을 지배하는 게 당연한데, 경보는 "망가졌다"고 폰에 울렸다.

### /stop이 실제로 듣는 곳 (그리고 안 듣는 곳)

협조적 중단은 **표시를 보는 루프에서만** 듣는다. 지금 보는 곳은 셋이다:

    수집        공고 한 건 사이 / 상세 조회 한 건 사이
    night-cycle 준비 한 건 사이
    (그 외)     안 본다 → `/stop 강제`가 유일한 방법

자기개선(`improve`)이 대표적으로 안 듣는 쪽이다. 안에서 `claude` CLI가
수 분씩 블로킹하고 기본 limit=1이라 "건 사이"라는 게 사실상 없다. 그래서
강제 종료는 프로세스 그룹째 보낸다 — 부모만 죽이면 `claude`가 고아로 남아
"멈췄다"고 답한 뒤에도 일이 계속 진행된다.

**대상을 골라 멈출 수 있어야 한다.** 처음엔 전부 멈추는 것 하나뿐이었는데,
검증 중에 지원준비를 접으려다 같이 돌던 수집을 두 번 날렸다(둘 다 상세
조회 중이라 수십 분어치였다). 지금은 `/stop 수집`, `/stop 12`로 고른다.

## 'AI 활용 경험'은 사람 몫이다 — 자동화가 안 건드린다 (2026-08-16)

원티드의 'AI 활용 경험' 칸을 **사용자가 사본메이커에 직접 등록해 뒀다.** 모든
이력서는 그 사본에서 복제되므로 이미 채워진 채로 온다. 그런데 우리 파이프라인은
공고마다 이 칸을 새로 써서 덮어쓰고 있었다 — 50자 한 줄 칸이라 매번 문장이
바뀌고, 사람이 확정한 문구가 사라졌다.

학력·링크를 사본에 맡긴 것과 같은 판단이다(`COPY_STEPS`): **공고에 따라 달라지지
않는 값은 다시 쓰지 않는다.** 다시 쓰는 것 자체가 위험만 늘린다.

- `assemble.py` — 편집기 스키마에서 `ai_usage`를 뺐고, 모델이 가이드 §6-1을
  읽고 되살려 넣어도 `_drop_manual_fields`가 지운다(프롬프트로만 막으면 샌다).
  50자 클램프(`_clamp_fields`/`FIELD_LIMITS`)는 쓸 데가 없어져 지웠다.
- `resume_editor.py` — 채우는 루프에서 뺐다(`MANUAL`). 셀렉터는 남겼다:
  칸이 사라졌는지 확인하고 자가수복이 고칠 수 있어야 한다.
- `resume-guide.md` §6-1 — "작성하지 않는다"로 다시 썼다. 근거 표는 남긴다.
  그 수치들은 간단 소개·주요 성과에 녹여 쓰는 사실 저장소다.

캐시된 조립 결과(`profile/generated/*.json`)에는 `ai_usage`가 남아 있지만
채우는 쪽이 읽지 않으므로 지우지 않았다.

## 이력서 원본은 편집도 금지다 (2026-08-16)

무인 준비가 `preview_resume_url`에 저장된 이력서 하나를 계속 재사용했다.
이력서가 계정에 쌓이는 걸 막으려던 것인데, 세 가지가 같이 깨졌다:

- `resume_url`을 넘기면 `_autoapply`가 `template=None`으로 바꿔 넘겨
  **`pick_template`이 통째로 무시된다.** 트랙별 사본메이커를 만들어 둔 의미가
  없었고, 무인 사이클은 그 분기를 한 번도 안 탔다.
- 재사용 대상이 '박예일 기본'으로 굳어 있었다. 스킬은 "사본은 비어 있다"를
  전제로 **추가만** 하므로(prune 없음), 같은 이력서를 공고마다 덮어쓰면 스킬이
  계속 쌓인다.
- 그 상태로 공고 33에 **실제 제출까지 나갔다**(apply_ledger id=4,
  resume_title='박예일 기본').

재사용을 지웠다. 그리고 `resumes.protect`를 **삭제 금지에서 편집 금지까지**로
넓혔다 — `fill()`이 보호 제목을 열면 `ProtectedResume`로 그 자리에서 멈추고,
`_submit`은 보호 이력서가 등록된 공고를 제출하지 않는다. 편집기는 자동저장이라
한 필드만 지나가도 되돌릴 수 없고, 사본메이커가 오염되면 거기서 뜬 사본이 전부
같이 오염된다. 아껴지는 이력서 몇 개와 바꿀 값이 아니다.

쌓이는 이력서는 `resumes --cleanup`이 치운다. 거기에 **사라진 이력서의 기록을
지우는 일**도 붙였다 — 사람이 플랫폼에서 직접 지우면 `made_resumes`에만 남아
"아직 있나?" 하고 헷갈리게 된다. `resume_builds`는 안 건드린다. 거기는 무엇을
만들어 무엇을 냈는지의 이력이고, 이력서가 사라졌다고 그 사실이 사라지진 않는다.

## 상주 리스너가 run.sh를 안 거치면 PATH에 claude가 없다 (2026-08-16)

`/guide` 요청이 "시작합니다" 확인만 오고 diff가 안 왔다. 원인 추적이 어려웠던
이유는 `com.autoapply.listen`이 `subprocess.Popen(stdout=DEVNULL,
stderr=STDOUT)`으로 `cli.py`를 fire-and-forget 실행하는데, 안에서 죽어도
그 출력이 전부 버려져서 아무 데도 안 남았다는 것이다.

`cli.py` 최상단에 잡히지 않은 예외를 텔레그램으로도 보내는 안전망을 먼저
넣고 나서야("cli.py 처리 안 된 오류") `ClaudeUnavailable: claude CLI를 찾을
수 없습니다`가 드러났다.

진짜 원인: `com.autoapply.night`/`flush`는 `run.sh`를 거치는데, `run.sh`가
`export PATH="$HOME/.local/bin:..."`를 스스로 하고 있었다. 반면
`com.autoapply.listen`은 `run.sh`를 안 거치고 launchd가 `python`을 직접
띄운다 — launchd 기본 PATH(`/usr/bin:/bin:/usr/sbin:/sbin`)엔 `claude`가
없고, 리스너가 띄우는 모든 서브프로세스(`/guide`, `/지원시작`, `/improve`,
수정요청 재작성)가 그 PATH를 그대로 물려받는다.

`com.autoapply.listen.plist`에 `EnvironmentVariables/PATH`를 넣어
`run.sh`와 같은 값으로 맞췄다. 검증은 리스너 프로세스의 실제 환경변수를
그대로 복제해 `cli.py guide`를 돌려 끝까지 통과하는 걸 확인했다(PATH 없이
`env -i`로 돌리면 바로 `ClaudeUnavailable`로 죽는 것도 대조 확인).

**교훈**: launchd 잡을 새로 추가할 때 `run.sh`를 거치지 않는 잡은 PATH를
따로 챙겨야 한다. 하나가 되면 나머지도 된다고 가정하면 안 된다.

## 이력서 정리는 '우리가 만든 것'만 지운다 (2026-08-16)

`resumes --cleanup`이 사람이 직접 만든 이력서를 지웠다
(`[포지션 리뷰] 코르카 Software Engineer`). 지원 기록에도 없고 로컬 사본도
없어서 되살릴 수 없었다.

원인은 기준의 순서였다. '오래되었거나 개수를 넘은 것'으로 고르면 **누가
만들었는지를 묻지 않는다.** 나이와 개수는 무엇을 지울지 정하는 기준이 아니라,
**우리 것 중에서 무엇부터 지울지** 정하는 기준이어야 했다.

이제 `made_resumes` 표가 소유의 근거다. `prepare_from_template`이 이력서를
만들자마자 적는다 — 채우다 실패한 것도 우리가 치울 물건이라 같이 적는다.
`resume_builds`로는 안 된다. 거기는 공고당 한 줄만 남아서(upsert) 같은 공고를
여러 번 만들면 예전 것이 기록에서 사라지고, 그러면 우리가 만든 이력서인데도
'모르는 것'이 되어 영영 안 지워진다.

# 남은 작업 큐

## 이력서는 사본에서 시작한다 (2026-08-16 도입)

`resumes.copy_from`이 트랙·키워드로 사본메이커를 고르고, 목록 ⋯ 메뉴의
'사본 만들기 → 이력서 제목 변경'으로 공고별 이력서를 만든다.

이걸로 한꺼번에 사라진 문제들:

- **완성도 71% 정체** — 학력(입력돼도 저장 안 됨)과 링크(입력칸 TimeoutError)가
  원인이었다. 둘 다 공고와 무관하게 늘 같은 값이라, 사본이 들고 오면 그만이다.
- **중복 제목** — '박예일 14'가 둘이라 지원 폼에서 무엇을 고르는지 알 수 없었다.
  이제 '{회사} {공고제목} #{n}'으로 붙이고, n은 DB 카운터(settings.resume_seq)에서
  받는다. 목록을 세지 않으므로 화면 파싱이 틀려도 번호가 겹치지 않는다.
- **스킬 정리(prune) 위험** — 사본 스킬은 비어 있어 추가만 한다.

검증(공고 79, 코드비전): 저장안됨 0, 플랫폼 미입력 0, 날짜 10/10, 스킬 12/12,
작성 완료, 학력·링크·언어 보존.

폴백은 남겨뒀다. 사본메이커가 계정에서 사라지면 빈 이력서 경로(`ALL_STEPS`)로
돌아간다 — 느리고 71%에서 멈출 수 있지만, 파이프라인이 서는 것보다는 낫다.

날짜는 갈라 다룬다. **재직기간**은 사본 값을 그대로 두고(회사에 다닌 기간은
공고에 따라 안 바뀐다), **성과 기간**은 매번 설정한다(성과는 공고마다 다시 쓴다).

### 확인했으나 문제가 아니었던 것
- '박예일 영업 사본메이커'가 **작성 중**인 것 — 사람이 '작성 완료'를 안 누른
  상태일 뿐이다. 완성도 패널의 미체크는 '글자수 400자 이상' 하나뿐이었고,
  우리 파이프라인은 간단 소개를 450자 이상으로 채운 뒤 finalize()를 부른다.
  비전이 '시험명* 미입력'이라고 보고했지만 그건 회색 템플릿 자리였다 —
  `vision.verify()`가 막아둔 오탐이 `ask()`에는 안 걸려 있었다(지금은 걸린다).
- **공고 9(트리플오스) `RecipeError: #name 요소를 찾지 못함`** — 원티드가
  공고/회사별로 지원 폼 레이아웃을 다르게 내려주는 게 아니었다. 같은 공고로
  `goto→click(지원하기)→expect(#name)` 체인을 5번 재현(autoapply 전체 1회 +
  체인만 4회)했는데 5/5 모두 #name을 정상적으로 찾았다. 대신 같은 재현에서
  **스크린샷 저장이 매번(지금까지 7/7) `waiting for fonts to load` 15~30초
  타임아웃으로 실패**했다 — 원래 사건의 스크린샷 실패도 같은 메시지였다.
  `session.py`가 브라우저 창을 화면 밖에 숨겨 띄우는 구조라(`hidden` 기본값)
  렌더링이 간헐적으로 느려지는 것으로 보인다(같은 조건에서도 소요시간이
  23초~67초로 널뛰었다). `recipes/wanted.json`의 `#name` expect timeout을
  15000→30000으로 올려 여유를 줬다(검증: 3회 추가 재현 모두 통과).
  **스크린샷 타임아웃은 미해결로 남긴다** — `session.py`의 `screenshot()`에
  `timeout=30000`을 줘봤지만 3/3 그래도 실패해서(원인이 폰트 로딩만이 아니라
  풀페이지 합성 자체가 느린 것일 수 있다) 되돌렸다. 아래 대기 큐 참고.

### 남은 것
- 경력 **삭제**는 아직 못 한다. 지금은 수정·추가만 한다. 사본에 담긴 경력이
  공고와 안 맞을 때 지우려면 행 삭제(🗑) 조작이 필요하다.
- biz/presales 트랙으로 실제 한 바퀴를 안 돌려봤다. 지금까지 검증은 dev뿐이다.
- ~~`resumes --cleanup` 기준을 실제로 돌려봐야 한다~~ — 2026-08-16 실행함
  (max_keep 31 / max_age_days 14). 지울 후보 0건, 우리 것 아닌 포트폴리오
  4건은 건너뜀, 사라진 이력서 기록 8건 정리. 사본메이커와 '박예일 기본'은
  `protect`에 있고, 그 목록은 이제 **편집 금지**이기도 하다.


자기개선 루프가 읽는 목록. **맨 위 미완료 항목 하나만** 하고, 끝나면 여기서
체크하고 커밋한다. 한 번에 여러 개를 하지 않는다 — 검토가 불가능해진다.

새 항목은 아래 규칙으로 넣는다:
- 무엇을 검증하면 끝난 것인지(`검증:`)를 반드시 적는다. 없으면 끝을 알 수 없다.
- 되돌릴 수 없는 동작(실제 지원 제출)은 이 큐에 넣지 않는다. 사람이 한다.

---

## 진행 중

(비어 있음)

## 대기

- [ ] **`llm.py`의 한도 감지가 영어 메시지를 놓친다** (2026-08-16 실측)
  `_raise_if_limited`가 "사용 한도", "한도에 도달" 같은 한국어 패턴만 본다.
  실측: `api_error_status:429`, `result:"You've hit your session limit ·
  resets 9:50pm (Asia/Seoul)"` 응답이 패턴에 안 걸려 일반 `RuntimeError`로
  샜다 — 한도인데 `UsageLimited`가 아니라 고장으로 기록된다는 뜻이다.
  `agent.py`가 한도와 고장을 구분해 처리하는 지점(재시도 대상인지, 자리를
  붙잡아두는지)이 있다면 거기서 오판할 수 있다.
  검증: 영어 한도 메시지로 `_raise_if_limited`를 단위 테스트하거나 재현해
  `UsageLimited`가 뜨는지 확인. 패턴에 `"session limit"`, `"hit your"` 등
  추가.

- [x] ~~**새 이력서가 '작성 중'에 머문다**~~ — 사본 경로가 들어오면서 해소됐다
  (사본은 만들어지는 순간 '작성 완료' 상태다). 임시 이력서 재사용으로
  되돌려 뒀던 것도 2026-08-16에 지웠다 — 그 재사용이 `pick_template`을
  무력화하고 기본 이력서를 덮어쓰던 원인이었다(위 `## 이력서 원본은 편집도
  금지다` 참고).

- [x] ~~**이력서 개수·기간 정리**~~ — 계정에 이력서 5개(기본 + 사본메이커 4)와
  포트폴리오 4개만 남았다. 시험 이력서는 사람이 지웠고, `made_resumes`에만
  남아 있던 기록 8건은 `resumes --cleanup`이 정리했다(이제 사라진 이력서
  기록은 스스로 정리된다). 남은 확인 하나는 TODO.md P1-B에 있다 —
  '박예일 기본'의 내용이 재사용 시절에 오염됐는지 사람이 봐야 한다.

- [ ] **텔레그램 400** — 링크를 넣은 캡션에서 400이 났다. HTML 앵커나 길이 문제로
  보인다. 메시지가 안 가면 승인 흐름 전체가 멈춘다.
  검증: 링크 포함 캡션이 정상 전송됨.

- [ ] **사람인·자소설 편집기 레시피**
  이제 셀렉터가 `recipes/<platform>.json`의 `editor` 섹션에 있으므로, 다른
  플랫폼은 파이썬을 고치지 않고 레시피 파일만 추가하면 된다.
  동작 방식(자동완성·날짜피커·호버추가)이 원티드와 다르면 그때 코드를 본다.
  검증: 사람인 이력서 편집기에 dry-run으로 필드 존재 확인.

- [ ] **레시피 빌더에 비전 연결**
  `vision.ask()`가 준비됐다("이 화면에서 제출 버튼의 라벨과 상태는?" 같은 질의가
  실제로 동작). 레시피가 깨졌을 때 화면을 읽고 셀렉터를 다시 만드는 루프에 쓴다.
  검증: 일부러 깨뜨린 레시피를 비전 단서로 고쳐 dry-run 통과.

비전 점검이 찾은 것들 (DOM 대조는 전부 놓쳤다):

- [ ] **링크 2개 이상**
  행 호버 → solid 아이콘으로 시도했으나 행이 안 늘어난다(1개에서 멈춤).
  덮어쓰기는 막았다. 학력과 같은 방식인데 왜 안 되는지 확인 필요.
  검증: GitHub·포트폴리오 둘 다 남음.

- [ ] **스킬 정리(prune)가 켜면 스킬을 없앤다** — 기본 꺼둠
  삭제 자체는 되고 새로고침 후에도 유지된다(칩 안 svg 클릭). 그런데 삭제 뒤에
  추가한 것이 저장되지 않아 11개 → 1개가 됐다. 누적보다 손실이 나쁘므로
  `skill.prune: "on"` 을 레시피에 넣어야만 동작하게 막아뒀다.
  다음에 볼 것: 삭제 후 추가 경로에서 저장 요청이 나가는지(날짜 칸과 같은 부류로
  같은 섹션의 다른 칸을 건드려야 PATCH가 나가는 구조일 수 있다).
  검증: 정리를 켠 상태에서 화면 스킬이 JSON과 일치.

  참고: 정리를 꺼도 실측상 큰 문제는 없었다. 미리보기 이력서를 덮어 쓰면
  이전 스킬이 대체로 밀려나고, 남는 것은 목표와 겹치는 항목이었다.

- [ ] **스킬 이름 매칭 보강**
  `REST API`·`GitLab CI/CD`·`AWS S3`가 원티드 DB에 없어 빠진다. 공백·슬래시가
  들어간 이름을 어떻게 매칭할지 정해야 한다.
  검증: `skills_skipped`가 비거나, 빠진 것이 화면 목록과 일치.


- [ ] **이미 쌓인 이력서 정리** (사람이 할 일)
  `박예일 2~6`은 안 쓰인다. `박예일 7`이 미리보기용이므로 남긴다.
  **삭제는 이 도구가 하지 않는다** — 계정 데이터를 지우는 건 되돌릴 수 없고,
  목록에 삭제 버튼도 없어 항목별 메뉴를 눌러야 한다. `cli.py resumes`로 목록을
  확인하고 원티드에서 직접 지울 것.

- [ ] **경력 2건 이상**
  학력은 행 호버 → 행 안쪽 solid 아이콘으로 해결됐다. 경력도 같은 방식일 것으로
  보이나 아직 확인 안 했다. 지금 경력이 1건뿐이라 검증할 데이터가 없다.
  검증: 경력 2건인 JSON으로 채웠을 때 둘 다 저장됨.

## 완료

- [x] **텔레그램 "/" 자동완성 목록 등록 + /apply로 통일 + 단독 "/"**
  (2026-08-16). `/지원시작`이 분명 구현돼 있는데 텔레그램에서 "/"를 쳐도
  안 보인다는 제보. 코드를 보니 `/지원시작`이 `_handle()`과 `/help`에 이미
  있었다 — **명령이 없는 게 아니라, `setMyCommands`를 한 번도 호출한 적이
  없어서** 텔레그램 클라이언트가 자동완성 목록 자체를 몰랐던 것이었다.

  `setMyCommands`는 command 필드에 영문 소문자·숫자·밑줄만 허용해 한글
  명령은 그대로 등록할 수 없다 — 영문 이름이 필요했다. 처음엔 두 이름
  (`/지원시작`·`/apply_start`)을 같이 받게 했다가, 사용자 지시로
  `/apply` 하나로 정리했다: 이 명령은 애초에 실제 제출을 하지 않는다
  (수집→지원준비까지고, 제출은 항상 폰 승인 버튼을 눌러야 나간다) —
  '지원시작'이라는 이름이 오히려 오해를 부를 수 있어 두 이름을 둘 이유가
  없었다. `/지원시작`은 이제 인식되지 않는다(모르는 명령으로 처리).

  덧붙여 **단독 "/"만 보내면**(자동완성 목록을 안 쓰고 그냥 슬래시만 친
  경우) "모르는 명령입니다" 접두어 없이 바로 도움말을 보여주게 했다 —
  `/help`와 동일 동작.

  `telegram.py`에 `BOT_COMMANDS`(12개, `apply` 포함) + `set_commands()`를
  추가하고 `setup()` 끝에서 자동 호출하게 했다(다음 최초 설정부터는 자동
  등록). 지금 계정은 이미 설정돼 있어 별도로 즉시 반영할 수 있게
  `cli.py telegram-commands` 명령도 추가.

  검증: `cli.py telegram-commands` 실행 → `setMyCommands` 200 OK →
  `getMyCommands`로 실제 텔레그램 서버에 12개(`apply` 포함) 등록 확인.
  `subprocess.Popen`을 patch해 실제 서브프로세스 없이 `_handle()`을 직접
  호출 — 단독 `/`가 HELP와 완전히 같은 텍스트를 내는 것, `/apply`는
  여전히 트리거되는 것(Popen 호출됨), `/지원시작`은 더 이상 안 먹히고
  "모르는 명령입니다"로 떨어지는 것까지 확인.

  (첫 검증 라운드에서는 patch 없이 `/apply`·`/지원시작`을 그대로
  호출해 실제 `night-cycle --target 1` 서브프로세스를 두 번 띄워버린 실수가
  있었다 — dry-run이라 실제 제출까지는 안 갔지만 실제 이력서 사본이
  계정에 남을 수 있었다. 그다음부터는 `subprocess.Popen`을 patch해서
  라우팅만 격리해 테스트했다.)

- [x] **텔레그램 답장이 연결 오류로 그 자리에서 사라지던 것 — 재시도로 해결**
  (2026-08-16). 사람이 봇에 명령을 보냈는데 답장이 안 왔다는 제보. 리스너
  로그(`data/logs/listen.err.log`)에서 원인을 특정했다: 21:07:39에 새 메시지를
  받았고(`getUpdates` offset 전진), 21:07:40에 `WARNING 텔레그램 알림 실패(무시):
  [Errno 54] Connection reset by peer`. `drain()`은 update_id를 **처리 성공
  여부와 무관하게 먼저 커밋**하므로(다음 getUpdates가 같은 메시지를 다시 안
  주게) 이 시점 이후로는 그 메시지를 다시 받을 방법이 없다. `_handle()`
  자체가 던진 예외 로그는 없어 — 명령은 정상 처리됐는데 답장 전송
  (`notify()` → `_call()`의 sendMessage)만 일시적 네트워크 오류로 실패하고,
  `notify()`는 그 실패를 삼키고 포기하도록 설계돼 있었다(본 파이프라인을
  막지 않으려는 의도였는데, 사람에게 가는 일회성 응답에는 그 설계가
  안 맞았다 — 헬스 알림과 달리 재발송 기회가 없다).
  `control_queue`·`settings`에 이 시각 기준 변화가 없어(개발지시 큐에도 안
  쌓임) 무엇을 보냈는지는 특정 못 했다 — 상태를 안 바꾸는 조회성 명령
  (`/status` 등)이었을 가능성이 높다.

  `telegram.py`의 `_call()`에 `httpx.HTTPError`(연결 오류·타임아웃·5xx)만
  겨냥한 재시도(최대 2회, 1.5초 간격)를 넣었다. `ok: false` 같은 영구 실패
  (잘못된 토큰 등)는 재시도해도 안 풀리므로 그대로 즉시 실패시킨다.

  검증: (1) 실제 텔레그램으로 전송해 정상 동작 확인. (2) `httpx.post`를
  가짜로 2번 실패시킨 뒤 재시도로 3번째에 실제 메시지 전송 성공까지 확인
  (`_call` 호출 3회, `time.sleep` 2회, 실제 message_id 반환). (3) 계속
  실패하도록 주입해 `notify()`가 예외 없이 `False`를 반환하고 기존과 같은
  경고 로그만 남기는 것 확인(회귀 없음).

- [x] **원티드 방문마다 서비스워커가 쌓이던 것 — attach 시점에 자동 정리**
  (2026-08-16). `braze-service-worker.js`가 `wanted.co.kr` 페이지를 열 때마다
  CDP 타깃 목록에 새로 하나씩 남고 안 없어지는 걸 위 스크린샷 버그를 파다가
  발견했다(71개 확인 후 페이지 하나 더 열었더니 72개). 스크린샷 타임아웃의
  직접 원인은 아니었지만(72개 정리해도 증상 그대로였음), 상주 브라우저가
  며칠씩 켜져 있는 구조상 무한정 쌓이는 별개의 누수였다.

  `PlaywrightSession._attach()`가 상주 브라우저에 붙을 때마다
  `_cleanup_stale_service_workers()`로 `type=service_worker` 타깃을 CDP
  `/json/close`로 정리하게 했다. 닫아도 안전하다 — 그 오리진이 필요하면
  브라우저가 알아서 다시 등록하고, 오프라인 캐시 등은 서비스워커 스레드가
  아니라 스토리지에 있어 지워지지 않는다.

  검증: 정리 전 3개 → 공고 3건 탐색(각각 새 `browser()` 호출, 매번 attach
  때 자동 정리) → 최종 1개로 확인. 로그에 "쌓인 서비스워커 2개 정리"가
  실제로 찍히는 것까지 확인 — 더 이상 방문 횟수만큼 무한정 늘지 않고
  매 attach마다 스스로 정리된다.

- [x] **증적 스크린샷이 매번 타임아웃으로 실패하던 것 — 자동 회복으로 해결**
  (2026-08-16). 원래 의심(폰트 로딩·풀페이지 합성 비용)은 전부 틀렸다.
  실측으로 밝힌 진짜 원인과 순서:
  1. 타임아웃을 15초→60초로 늘려도 **정확히 그 값만큼 걸리고 실패** —
     느린 게 아니라 새 프레임을 아예 못 만드는 상태.
  2. `about:blank`·`example.com` 같은 빈 페이지도 동일 증상 — 원티드
     페이지의 폰트·콘텐츠 문제가 아니었다.
  3. 누적된 서비스워커 72개(방문마다 하나씩 쌓이고 안 없어짐 — 이것도
     별도 결함, 아래 대기 큐 참고)를 정리해도 무관했다.
  4. `pmset -g assertions`로 확인 — 화면 잠금·디스플레이 절전 둘 다
     아니었다(오히려 Chrome이 `NoDisplaySleepAssertion`을 쥐고 있었다).
  5. Chrome 실행 인자에 `--enable-unsafe-swiftshader`(소프트웨어 렌더링
     폴백)와 `--enable-features=CDPScreenshotNewSurface`가 걸려 있었고,
     상주 창이 5시간 넘게(여러 날에 걸친 프로필 재사용 누적) 켜져 있었다.
     `chrome://gpu` 조차 빈 텍스트로 돌아와 — 렌더러 자체가 맛이 간
     상태로 결론.
  6. **껐다 켜니 즉시 회복**(재현 3/3, 2~4초로 정상화, 실제 화면 캡처로
     내용도 정상 확인).

  코드 반영(`session.py`): `screenshot()` 실패 시 죽이지 않고
  `flag_needs_restart()`로 표시만 남긴다(그 시점엔 다른 스텝이 같은
  브라우저를 쓰고 있을 수 있어서). **다음 `PlaywrightSession.start()`**
  (새 세션을 여는, 아무 작업도 안 걸려 있는 안전한 경계)가 그 표시를
  보고 `restart_resident()`로 껐다 켠 뒤 이어간다. 수동용으로
  `cli.py browser-restart`도 추가.

  검증: 플래그를 강제로 남기고 `browser()`를 새로 열어 — 로그에 "재시작
  신호 발견" → 실제 PID가 바뀜(65426→69993) → 플래그 소비(파일 삭제)
  → 스크린샷 1.7초 성공까지 전 구간 실행으로 확인. `cli.py browser-restart`
  단독 실행도 확인.

- [x] **포트폴리오 매칭 지원** (2026-08-16) — 이력서(`job.resume`)와 별개로
  공고에 맞는 포트폴리오를 에이전트가 골라 지원 폼에서 같이 체크한다.
  `src/autoapply/portfolio.py`(매칭) + `resume_builds.portfolio_title`(캐시,
  72시간) + `cli.py _job()`의 `job.portfolio`/`job.portfolio_nfd` +
  `recipes/wanted.json`의 새 check 스텝(`optional: true`).

  실측(공고 362772, 유에스소프트)으로 두 가지를 확인했다:
  1. **포트폴리오는 이력서와 같은 첨부파일 체크리스트에 있다** — 별도 섹션이
     아니다. 여러 개(이력서 + 포트폴리오)를 동시에 체크해도 서로 안 밀어낸다
     (`li input[type=checkbox][id^="resume-"]` 구조 공유, 실측으로 다건 동시
     체크·제출버튼 활성화까지 확인).
  2. **포트폴리오 파일명만 유니코드 NFD로 렌더링된다** — 이력서 제목(사본메이커
     등, 사람이 직접 입력)은 NFC인데 업로드형 문서(PDF)는 NFD다. `text-is()`는
     바이트 비교라 NFC 그대로 넣으면 셀렉터가 못 찾고, `optional` 스텝이라
     **에러 없이 조용히 건너뛴다** — 사람은 포트폴리오가 빠진 걸 모른다.
     그래서 `job.portfolio`(NFC, 표시·저장용)와 `job.portfolio_nfd`(NFD,
     셀렉터용)를 분리했다(`portfolio.to_selector_text()`).

  포트폴리오 메타데이터(제목·요약)는 `config.yaml`의 `portfolios.catalog`에
  뒀다 — 실제 계정에 업로드된 4개(AX개발자·풀스택·데브옵스·개발자용 종합)를
  `cli.py resumes`로 확인하고, 요약은 `~/포트폴리오/*.md` 원본을 읽어 적었다
  (지어내지 않음). 전부 개발 트랙이라 biz/presales 공고는 매칭 없음(None)이
  정상 — 카탈로그가 없으므로.

  검증:
  - LLM 매칭 3건 전부 실제 호출로 확인(DevOps 공고→데브옵스 포트폴리오,
    AI/LLM 공고→AX개발자 포트폴리오, 해외영업 공고→매칭 없음/None). 세
    건 다 정답 — 세 번째는 카탈로그가 전부 개발 계열이라 "해당 없음"이
    맞는 답이었는데, 모델이 억지로 끼워 맞추지 않고 정확히 그렇게 답했다.
    (첫 시도는 계정 세션 사용한도(429, "resets 9:50pm")로 막혔었고, 그
    과정에서 `llm.py`의 `_raise_if_limited`가 한도 문구를 한국어 패턴으로만
    잡아 영어 메시지("You've hit your session limit")를 놓치는 별도 결함을
    발견해 기록해뒀다 — 아래 대기 큐 참고. 한도가 풀린 뒤 재시도해 통과)
  - 실제 코드 경로(`cli.py _job()` → `apply.py _run_with()` → 실제 원티드
    페이지)로 dry-run 실행: 이력서·포트폴리오 체크 스텝 둘 다 "선택 스텝
    건너뜀" 로그 없이 통과, 제출 버튼 활성화(`5:expect` 통과), `6:submit`은
    dry-run이라 스킵. 테스트에 쓴 `resume_builds` 더미 행은 실행 후 삭제.
  - 스크린샷(비전 점검)은 시도했으나 기존에 알려진 폰트로딩 타임아웃 버그로
    실패(45초로 늘려도 동일) — 새로운 결함이 아니라 위 대기 큐 항목과 같은
    원인으로 보고 로그 기반 검증으로 대신함.
  - **`assemble.build_editor_json(283)`을 실제로 돌려 매칭→저장 전 구간
    확인**(41초, 실측 벤치마크와 일치). `portfolio_title: 박예일_포트폴리오_
    풀스택.pdf`가 나왔고(RFID 개발 공고라 특정 분야 특화보다 종합·풀스택
    쪽이 맞는 합리적 선택), `resume_builds`를 직접 조회해 **진짜 저장 경로로**
    남았음을 확인(수동 SQL이 아니라 `_save_build()`를 거친 값). 이 공고는
    required_gaps=3(ok=False)이라 실제 지원까지는 안 갔지만, 그건 이력서
    작성 쪽 판정이지 포트폴리오 매칭과는 무관하다.

- [x] 지원 속도 상한 (`claim()`에서 일일/실행 단위 차단)
- [x] Playwright 러너 + dry-run 기본값
- [x] `recipes/wanted.json` — 실제 제출로 검증
- [x] 세션 생사 자동 감지 (URL 리다이렉트 기준)
- [x] L1 이상 감지 (LLM 0회)
- [x] 파이프라인 A/B 분리 + launchd 2시간 주기
- [x] 이력서 어셈블러 (작성 → 검수 → 보강)
- [x] 편집기 자동 입력 — 완성도 100%
- [x] 스킬 자동 등록
- [x] **이력서 품질 결함 5건** — 프롬프트 유출, 내용 분산, 학력 누락, 언어 미설정,
      AI 활용 잘림. 자세한 원인은 커밋 메시지 참조.
- [x] **학력 다건** — 진입점은 행 호버 시 나타나는 아이콘 버튼. 행 안쪽으로
      범위를 좁혀야 한다(페이지 전체로 잡으면 14개).
- [x] **스케줄러 시각 결합 제거** — launchd(짝수 시각)와 run.sh(3/9/15시)의
      교집합이 없어 수집이 한 번도 안 돌았다. "마지막으로 언제 했냐"로 바꿨다.
- [x] **같은 공고 반복 준비** — 최근 24시간 내 조립한 공고는 대기열에서 뺀다.
      dry-run은 선점하지 않으므로 이게 없으면 1위만 계속 준비된다.
- [x] **미리보기 이력서 재사용 + 제출 경로 교정** — 캡션이 `apply --live`를
      안내했는데 그건 이력서를 다시 채우지 않고 제목으로 고르기만 한다.
      다음 사이클이 덮어쓰면 다른 공고용 이력서가 제출된다.
      `autoapply --live`로 바꿔 제출 직전에 그 공고용으로 다시 채우게 했다.
- [x] **링크 칸이 스킬 직후 안 열리던 것** — 스킬 팝업이 링크 행 클릭을 가로챈다.
      Escape도 blur도 못 닫고, **다른 섹션 제목을 실제로 클릭**해야 닫힌다.
      검증: 한 실행에서 스킬 12개 + 링크 2개 동시 통과.
- [x] **준비된 지원서를 폰으로 전송** — 성공이면 채워진 폼 **스크린샷**, 실패면
      이유. 셀렉터나 로그를 보내는 건 의미가 없다 — 사람은 사진으로만 판단할 수
      있다. 검증: 실제 전송 확인(sendPhoto True).
- [x] **`cycle-apply`** — 대기열 상위 N건을 dry-run으로 준비하고 사이클에 연결.
      cron에서는 절대 제출하지 않는다. 텔레그램 `/pause`로 멈출 수 있다.
      검증: 실제 대기열 공고(인졀미)로 6스텝 통과, 정지 상태에서 건너뜀 확인.
- [x] **조립 결과 재사용** — 72시간 내 결과는 재사용. 40초 → 0.0초, LLM 0회.
- [x] **학력 종료일 되돌아감** — 원인은 저장 요청이 아예 안 나가는 것이었다.
      날짜 피커는 화면만 바꾸고 저장을 트리거하지 않는다. 같은 항목의 텍스트
      칸을 다시 건드리면 `PATCH .../educations/{id}`가 나가며 날짜까지 저장된다.
      검증: 날짜 12개 전부 JSON과 일치.
- [x] **주요 성과 다건** — 4건 채움 확인(제목 4/4 일치). '주요 성과 추가'로 칸을
      만들되 채울 내용이 있는 만큼만 만든다. 날짜 슬롯은 성과 개수에 따라
      뒤로 밀리므로 고정 인덱스가 아니라 계산으로 구한다.
- [x] **스킬 표기 폴백** — `AWS(EC2, S3)` → `AWS`. 괄호를 떼고, 안의 항목도
      후보로 시도한다. 검증: `skills_skipped` 빈 목록, 12개 전부 등록.
- [x] **학력 종료일** — 앞선 '확인' 버튼 수정으로 이미 해결돼 있었다.
      당시 검증이 오래된 화면 상태를 봤을 뿐. 날짜 6개 전부 통과 재확인.
- [x] **누락 스킬 보고** — `skills_skipped`로 낸다. 실측에서 `AWS(EC2, S3)` 검출.
- [x] **`gaps` 되먹임** — `resume_builds` 테이블에 조립 결과를 남기고 판정이
      `REQUIREMENT_GAP` blocker로 읽는다. 검증: 필수 미충족 5건 주입 →
      `v_actionable`에서 제외 확인.
- [x] **`RESUME_MISSING` 제거** — 공고마다 이력서를 만드니 미리 준비해둘 이유가
      없어졌다. 이걸 걷어내자 자동지원 가능이 0 → 133건.
- [x] **`autoapply` 체인** — 조립 → 편집기 등록 → 지원 폼까지 한 명령.
      이력서 제목이 고리다. 편집기에서 제목을 읽어 레시피에 넘기므로
      `applicability.resumes` 매핑이 필요 없어졌다 (resumes.wanted.dev = null인
      상태로 dry-run 6스텝 전부 통과).
- [x] **'작성 완료' 단계** — 완성도 100%여도 누르지 않으면 '작성 중'으로 남아
      지원 패널 목록에서 고를 수 없다. 체인이 정확히 여기서 끊겼었다.
- [x] **주요 성과 기간** — 스키마에 achievements.start/end 추가.
      실측: 성과 2024.01–2024.02가 재직기간 2023.08–2024.04 안에 정상 배치.
      날짜 6개 중 5개 검증 통과 (학력종료는 별도 항목으로 분리).
- [x] **링크 칸 자동 입력** — 3개 등록, 완성도 100% 확인.
      안내 문구로 여는 방식은 부분 입력 후 문구가 바뀌어 깨진다.
      해시가 아닌 `.link-view` 클래스로 행을 잡는다.
- [x] **회사명 자동완성 글자 유실** — 검증 통과 (회사명·학교명·전공 3/3 원본 일치).
      후보가 없을 때 Tab으로 확정되고, 값이 다르면 재입력하는 교정 경로가 잡아준다.
      잘리는 위치가 매번 달라 재시도만으로는 못 고치므로 **대조 검증이 본체**다.
