## 리스너가 6시간 죽어 있었는데 로그는 0바이트였다 (2026-08-17)

사람이 "텔레그램이 죽은 것 같다"고 해서 알았다. 실제로 죽어 있었고, 폰에서 보낸
명령 4건이 응답 없이 쌓여 있었다(`cli.py listen`을 손으로 부르니 `handled: 4`).

    launchctl print gui/501/com.autoapply.listen
      state          = spawn scheduled
      runs           = 418
      last exit code = 78: EX_CONFIG
      properties     = ... | penalty box | inferred program

일어난 일:

    12:02  .venv가 없는 상태에서 잡이 bootstrap 됐다(심볼릭 링크 사고 직후
           복구 중이었다). launchd의 posix_spawn이 ENOENT로 실패
    12:06  .venv가 다시 생겼다 — 그런데 KeepAlive가 못 살렸다.
           418번 재시도하고 418번 같은 자리에서 죽었다
    18:16  사람이 발견

### 왜 아무도 몰랐나 — 프로세스가 안 뜨면 로그를 쓸 주체가 없다

`listen.out.log`·`listen.err.log`가 **정확히 0바이트**였다. 스폰 단계에서 실패했으니
당연하다. 그런데 이 시스템에서 "조용함"은 원래 정상의 모습이다 — 롱폴링 리스너는
할 일이 없으면 아무것도 안 쓴다. **고장과 정상이 파일 크기까지 똑같았다.**

`health.py` 앞머리에 적어둔 문장 그대로였다: "사람이 안 보는 시스템에서 '아무 일도
안 일어남'과 '망가져서 아무 일도 못 함'은 겉보기에 같다." 그때는 파이프라인 숫자를
봤는데, 정작 그 숫자를 실어나르는 프로세스 자체는 아무도 안 보고 있었다.

### KeepAlive는 스폰 실패에서 못 빠져나온다 (실측)

`launchctl kickstart`를 걸어도 안 돌아왔다. `bootout` + `bootstrap`이라야 살아났다.
실행파일이 4분 뒤 제자리에 돌아왔는데도 그랬다 — launchd가 없는 경로에 감시를
걸어둔 채 penalty box에 넣어놓고, 그 감시가 다시 안 울렸다.
그래서 `watchdog.sh`는 kickstart를 아예 안 쓴다.

### 고친 것 — 두 겹

**1. 모든 잡의 진입점을 `/bin/bash`로.** ProgramArguments[0]이 `.venv/bin/python`이나
`schedule/run.sh`면 그게 없을 때 launchd 스폰이 실패하고, 그 실패는 로그에 한 글자도
안 남는다. `/bin/bash`는 없어지지 않으므로 스폰은 **언제나** 성공하고, 무엇이 없는지가
스크립트 손으로 로그에 찍힌다(`schedule/listen.sh` 신설, run.sh 계열 3개는 인자로 이동).
종료코드도 launchd의 78이 아니라 스크립트 자신의 것이 된다.

**2. `com.autoapply.watchdog` (5분 주기).** 리스너가 살아 있는지 보고, 아니면
bootout+bootstrap으로 되살리고 폰으로 알린다. 판정 둘:

    launchctl state != running          → 프로세스가 없다
    listen_heartbeat가 600초째 안 움직임 → 떠는 있는데 진행이 없다

심장박동은 `watch()`가 롱폴링 한 바퀴(25초)마다 `settings`에 **에폭 초로** 적는다.
읽는 쪽이 bash라서 그렇다 — 이 저장소의 `now()`는 `+09:00` 꼴인데 BSD `date`의 `%z`가
콜론을 못 읽는다. 감시자가 파이썬을 못 쓰는 상황(=`.venv`가 사라진 상황)을 위해 만든
장치인데 파싱에 파이썬이 필요하면 앞뒤가 안 맞는다. 그래서 감시자는 `sqlite3`와
`curl`만 쓴다. 둘 다 macOS에 원래 있다.

**맥이 자다 깬 것과 고장을 가른다.** 잠들었다 깨면 프로세스는 멀쩡한데 심장박동만
낡아 있다. 별도 API를 쓰지 않고 **감시자 자신이 몇 틱을 건너뛰었는지**로 잰다
(`watchdog_last_tick`) — 자는 동안엔 감시자도 안 돌았으니 그 공백이 곧 수면 시간이다.
공백이 3틱을 넘으면 그 틱의 심장박동 판정은 건너뛴다.

검증(4경로 실주행): 잡을 bootout → 감시자가 잡아내 되살리고 텔레그램 발송 ✓ /
심장박동만 700초 낡게 → 프로세스 살아 있어도 재시작(PID 73566→73898) ✓ /
`watchdog_last_tick`을 1시간 전으로 → 낡은 심장박동을 무시하고 "정상" ✓ /
`.venv` 없는 사본에서 `listen.sh` → 종료코드 1과 복구 안내가 로그에 남음 ✓

배터리에서도 안 건너뛴다(`run.sh`와 다른 점). 롱폴링은 유휴 비용이 거의 없고,
리스너가 죽어 있으면 폰에서 보낸 명령이 그냥 사라진다.

---

## 실제 제출물을 만드는 경로에 검수가 없었다 (2026-08-17)

`llm_calls`를 세어보고 알았다:

    phase='to_editor_json'  sonnet-5  36회   ← 지원에 실제로 나가는 이력서
    phase='review'          haiku-4.5  2회   ← 저장소 통틀어 2번

`review()`는 `assemble.build()`(마크다운 경로)에만 있었고, 그 경로는 사람이
`cli.py resume`로 부를 때만 쓴다. **실제 지원 경로(`build_editor_json`)에는 보는 눈이
아예 없었다.** PORTFOLIO.md·TODO.md가 "write → review → 재작성"을 파이프라인으로
그려둔 것과 코드가 어긋나 있었다.

그 사각지대에서 실제로 나간 것들(6개 공고 벤치마크):

    공고 121  사실 저장소에 없는 `Spring Boot`가 스킬로 등록됨 (required_gaps 2 → ok=True)
    공고 119  써본 적 없는 제품의 사용 후기를 지어냄. 그 공고는 "허위사실이 있는 경우
              합격 취소"를 명시하고 있었다
    공고 4110 경력 도메인을 `위탁판매` → `부동산 위탁판매`로 늘림 (공고가 부동산 도메인을 요구)
    공고 310  §3.5에 없는 `엑셀·구글 시트·구글 슬라이드`를 스킬로 등록

### 스킬은 코드가, 산문은 LLM이 본다

검수 A/B(Haiku vs Sonnet 5, 실측 불량 3건 + 대조군 1건)에서 **두 모델 다 스킬 대조에서
헛발질했다**:

    Haiku   §3.5에 **있는** `Express`를 "없다"며 날조로 지목
    Sonnet  §3.5에 **있는** `Docker`·`AWS`를 "본문에 근거 문장이 없다"며 날조로 지목

§3.5는 고정된 단어 목록이라 물어볼 일이 아니었다. `unknown_skills()`로 가이드 전문과
문자열 대조하니 4/4 정확·오탐 0이다(`Spring Boot`·`엑셀` 잡고, 경력 설명에만 있는
`Tesseract OCR`·`OPC UA`는 통과). LLM에는 대조로 안 되는 것 — 산문 속 경험 날조 — 만 맡긴다.

### 검수 결과로 자동지원을 막지 않는다

산문 날조는 두 모델 다 3/3 잡았다. 차이는 정밀도였다:

    Sonnet 5  공고 4110에서 지적 1건 / 그게 정확히 그 문장. 대조군 오탐 1건
              (`클라우드 아키텍처 설계 참여` — §3.3보다 **겸손하게** 쓴 표현이다)
    Haiku     같은 건에서 날조 2건 중 하나가 §5 **확정값**(`월 10건 → 0건`)이었다.
              빈 `[형식]` 줄, `✓` 체크리스트 16줄 같은 잡음도 냈다

**둘 다 4건에 1건씩 잘못된 날조를 단다.** 그걸 차단에 쓰면 멀쩡한 이력서가 선다.
되돌릴 수 없는 지점(제출) 앞에는 이미 사람이 있으므로, 판단이 갈리는 지적은 그 사람에게
보낸다 — `resume_builds.review_notes` → 승인 캡션의 ⚠️. 모델은 정밀도를 보고 Sonnet 5로
정했고(`llm.editor_review_model`), 마크다운 경로의 Haiku는 그대로 뒀다.

재작성은 **날조가 있을 때만 1회**다. 재작성은 조립 전체를 다시 받는 것이라 건당 30초·
2,500토큰이고, 두 번 돌려도 세 번째가 필요해지는 종류의 문제(근거가 정말 없는 요건)는
재작성으로 안 풀린다.

### 곁가지: 토큰 기록이 입력을 거의 다 놓치고 있었다

`llm_calls.input_tokens`에 늘 한 자릿수가 찍혀 있었다. claude CLI는 프롬프트 대부분을
캐시로 처리하고 그 양을 **따로** 담기 때문이다:

    실측(review_editor, 프롬프트에 가이드 33KB 포함)
      input_tokens                    2
      cache_read_input_tokens     7,884
      cache_creation_input_tokens 22,757   ← 실제 입력의 대부분이 여기 있었다
      output_tokens                 577
      total_cost_usd             0.1672

`cache_read_tokens`·`cache_write_tokens` 컬럼을 만들어 합계와 내역을 같이 남긴다.
**비용은 여전히 CLI의 `total_cost_usd`를 쓴다** — 세 종류(기본 입력·캐시 쓰기·캐시 읽기)와
출력의 단가가 다 달라서 토큰 합으로 환산하면 틀린다. `cli.py llm-cost`에 그 주의를 적어뒀다.
과거 행은 캐시 칸이 비어 있다(소급 복구 불가).

여기서 보이는 것: **비용의 대부분이 가이드를 캐시에 쓰는 값**이다(22,757 / 30,643).
조립과 검수가 같은 가이드를 쓰는데도 캐시 쓰기가 다시 일어나는 건 두 프롬프트의
**앞부분이 달라서** 프리픽스가 안 맞기 때문이다. 프리픽스를 맞추면 검수 비용이 크게
내려갈 여지가 있다 — 아직 안 해봤다(아래 대기 큐).

### 곁가지: 측정 도구가 먼저 고장 나 있었다

첫 A/B에서 Sonnet이 `Spring Boot` 날조를 못 잡은 것으로 집계됐는데, 실제로는 잡았고
**내 파서가 못 읽었다** — Sonnet은 등급표를 `` - `[날조]` … `` 처럼 백틱으로 감싸 온다.
그러면 전부 '형식'으로 떨어지고, 자동지원을 막는 유일한 등급인 '날조'가 통째로 사라진다.
`LEVEL_TAG` 정규식으로 마크다운을 허용해 고쳤다. **검수를 차단에 쓰지 않기로 한 판단이
이 종류의 사고를 덜 위험하게 만든다** — 파싱이 또 깨져도 사람이 보는 목록이 비는 것으로 끝난다.

## 직무명 표가 공고의 자격요건을 가렸다 (2026-08-17)

`resume-guide.md` §7이 **직무명 → 1·2·3순위** 고정표였다. 공고 4110(젠스타파트너스
`Back-end Engineer(신입)`)은 자격요건에 Docker·AWS·인프라가 나란히 있는데, 표는
'백엔드' 행 하나로 (B)(A)(C)를 정해버려 **(D) 배포 자동화와 (E) AWS 마이그레이션이
이력서에 아예 안 들어갔다.** 국내 공고는 직무명과 자격요건이 자주 어긋나는데, 그
어긋남만큼이 통째로 버려지고 있었다.

순서를 정하는 주체를 뒤집었다. **자격요건 → §3 근거 매핑 → 필수요건을 많이 덮는
순**이고, 기존 표는 §7-3 '요건이 거의 안 적힌 공고'용 기본값으로 내렸다.
실측(공고 4110 실제 조립 2회): 성과 순서가 `AWS 마이그레이션 → 배포 자동화 →
동시성/SaaS`로 뒤집혔다.

### 간단 소개에 대표 프로젝트 링크를 넣는다

플랫폼 링크 칸은 **사본이 들고 오는 고정값**이라(COPY_STEPS에 링크가 없다) 공고마다
바뀌지 않는다. 그래서 *이 공고에 맞는 저장소*를 보여줄 자리가 간단 소개뿐이었다.
§8 블록(100~300자 + URL 한 줄)을 추가하고 간단 소개 목표를 450 → 550~850자로 올렸다.

두 가지를 프롬프트가 아니라 코드로 막았다:

    지어낸 URL   가이드 §3.4 표에 없는 주소는 줄째로 지운다(_strip_unknown_links).
                 모델이 저장소 이름에 프리픽스를 붙여 만들면 404가 나가는데,
                 사용자 이름으로 나간 죽은 링크는 확인할 방법이 없다.
    보강이 삼킴  글자수 보강은 간단 소개 **전체**를 새로 받는다. `[지원동기]` 같은
                 블록이 조용히 사라지길래 머리표로 대조해 되붙인다(_restore_blocks).

링크 검사는 보강 **앞뒤로 두 번** 한다. 앞에서만 하면 보강이 새로 지어낸 주소가
그대로 저장되고, 뒤에서만 하면 링크 줄이 지워지며 글자수가 최소치 밑으로 떨어져도
다시 받을 기회가 없다.

### 이력서로는 못 하는 요구는 폰 승인 화면에만

공고 4110은 "기본정보 미기재 시 서류 검토 없이 불합격", "사전과제" 같은 조건을 달고
있다. 이런 건 이력서 본문에 적어도 충족되지 않는다 — 적으면 **하지 않은 일을 했다고
쓰는 것**이 된다. `manual_todos`(resume_builds 컬럼)로 갈라 승인 캡션에만 띄운다.
조립과 알림이 다른 프로세스라 DB를 거쳐야 하고, 캐시된 조립을 재사용해도 같은 안내가
나가야 한다.

**포트폴리오 첨부는 여기 안 넣는다.** `portfolio_match`가 공고를 읽고 골라 지원 폼에서
자동으로 첨부하므로 사람이 할 일이 아니다. 실측으로 한 번 올라왔고(첫 실주행),
프롬프트에 더해 `AUTOMATED_TODO`로도 막았다 — 할 일 없는 안내가 매번 뜨면 그 목록
전체가 무시되고 진짜 할 일까지 묻힌다.

### 곁가지: §7 표가 없는 사실을 가리키고 있었다

AI/LLM 행의 2순위가 `(F) Gemini 챗봇`이었는데 §3 사실 저장소에 (F)가 없다. 언제
지워졌는지는 못 찾았다(`profile/`은 git 밖이고 `guide-backups/`도 비어 있다).
참조를 걷어내고 §6 미확보로 옮겼다 — 근거가 없는 항목이 우선순위표에 남아 있으면
모델이 그걸 근거로 삼는다. `3.5 스킬`의 `Gemini API`도 같은 자리에서 온 것으로 보여
같이 보류로 표시했다.

## 고장이 조용히 지나가고, 지원준비가 무한히 다음 공고로 흘렀다 (2026-08-17)

아침에 "새벽에 아무 일도 안 일어났다"로 발견한 사건 하나에서 다섯 가지가
같이 드러났다. 공통점은 **실패가 실패로 보이지 않았다**는 것이다.

### 폰에서 부른 작업은 로그가 통째로 사라졌다

리스너가 띄우는 모든 자식이 `stdout=DEVNULL, stderr=STDOUT`이었다.
`stderr=STDOUT`은 "stdout과 같은 곳"이라는 뜻이라 **stderr까지 /dev/null로
갔다.** 스케줄 잡(night/scrape/flush)은 plist의 `StandardErrorPath`가
받아주는데 폰에서 부른 것만 사라지는 비대칭이었고, 그 비대칭이 원인 추적을
두 번 막았다(2026-08-16 `/guide` → ClaudeUnavailable, 2026-08-17 지원준비 →
claude 실행 실패). `tasks.spawn()`으로 모아 `data/logs/<이름>.log`에 남긴다.

### 한도·로그인 만료가 큐에서 조용히 버려졌다

`UsageLimited`가 `errors.SKIP_TYPES`에 있었다. 주석은 "전용 경로가 있다
(orchestrator._one)"였는데 **그 함수는 진작 사라졌고**, 남아 있는 전용 경로는
`plan()`/`execute()` 둘뿐이었다. 이력서 작성 경로(`llm.ask`)에는 없어서,
한도에 걸리면 최상단까지 올라와 SKIP으로 버려지고 프로세스가 죽었다.

큐에 안 넣는 판단은 맞다 — 여기에 고칠 코드가 없다. 그래서 `BLOCKED_TYPES`로
갈라 **큐는 건너뛰되 원인은 반드시 보낸다**(같은 이유로 1시간에 한 번).

**로그인 만료를 한도에서 떼어냈다(`llm.LoginExpired`).** 회복 방식이 정반대다:

    한도       기다리면 풀린다     → 다음 새벽 사이클이 이어받으면 된다
    로그인만료  기다려도 안 풀린다  → 매 사이클 같은 자리에서 죽으며 자리만 태운다

예외형으로는 구분이 안 온다 — 둘 다 exit 1 + 메시지로만 온다. 메시지에서
가르되 **한도보다 먼저 본다**(두 문구가 한 봉투에 같이 오는 경우가 있다).

무인 실행에서 만료를 아예 안 겪으려면 `claude setup-token`(1년짜리 OAuth
토큰)을 받아 launchd plist의 `CLAUDE_CODE_OAUTH_TOKEN`에 넣는다. 이 토큰은
`/login` 자격증명보다 우선한다. **아직 안 했다 — 사람이 브라우저 승인을
해야 하므로 대기 큐에 있다.**

### 실패는 목표를 못 채우므로 루프를 못 멈춘다

`while prepared < target`에서 실패는 `prepared`를 안 올린다. 멈추는 조건이
셋뿐이었다 — 사람 중단 / 대기열 소진 / 같은 job_id 재등장. claude 하나가
죽으면 **대기열이 빌 때까지 계속 다음 공고로 갔다.** 그날 앞에 actionable이
122건 있었다. `--target 1`에도 122번 시도한다는 뜻이다.

같은 오류(지문 기준)가 3번이면 접는다. 한 번은 그 공고만의 사정일 수 있고,
두 번은 우연일 수 있다. LLM을 못 쓰는 실패는 세지 않고 **즉시** 접는다 —
다음 공고도 확실히 같은 자리에서 죽으므로 셋을 셀 이유가 없다.

PAUSE는 안 건다. 자동 정지시키면 다음 새벽이 통째로 안 돌고 사람이 /resume을
누르기 전까지 조용히 서 있는다 — 지금 고치려는 증상과 같은 모양이다.

### /apply가 정지 중인데 "시작합니다"라고 답했다

정지 여부를 안 보고 night-cycle을 띄운 뒤 "🚀 시작합니다"로 답했다.
night-cycle은 안에서 `is_paused`에 걸려 0초에 죽었고 출력은 /dev/null이었다.
사람 쪽에서는 "시작한다더니 영영 아무 일도 안 일어난다"뿐이다. 실측 근거:
`running_tasks` #15·#19·#21이 시작·종료 시각이 같은 0초짜리다.

자동으로 풀지는 않는다 — 정지를 건 주체가 둘이고(사람 `/pause`, 자가복구
보류) 무게가 다르다. 대신 `resumerun:N` 버튼 하나로 "풀고 시작"을 합쳤다.

### /stop이 한 건을 다 끝내야 들었다

중단 표시를 while 루프 꼭대기에서만 봤는데, 한 건은 preflight + 조립(LLM,
호출당 최대 900초) + 등록(브라우저) + 지원 폼이 한 덩어리다. 실측:
`running_tasks` #22가 `cancel_at` 07:00:40 → 종료 07:01:44. 표시는 붙었고
동작했다. **늦었을 뿐이다** — 그리고 같은 시각 리스너가 "텔레그램 수신 실패
— timed out"을 9번 연속 내고 있어 도착도 늦었다.

이제 네 곳에서 본다: 지원준비 시작 전 · 조립 중 · 등록 전 · 지원 폼 전.
**`fill()` 안에서는 여전히 안 본다** — 편집기는 자동저장이라 중간에 끊으면
절반만 채워진 이력서가 계정에 남는다. 최악의 대기는 fill 한 번이다.

곁가지로, `_cycle_apply`가 `Cancelled`를 한 공고의 실패로 삼키고 있었다.
그러면 멈추라고 한 뒤에 다음 공고가 시작된다 — 사람 눈에는 그게 "안 멈춘다"다.

### 끊기면 마지막 단계에서 이어받는다

`resume_builds.stage`(assembled / filling / registered / prepared). 재사용
규칙이 전부다:

    registered·prepared  fill()을 다시 안 부른다. 사본 스킬 채우기는 "비어
                         있다"를 전제로 추가만 하므로(prune 없음), 다시 채우면
                         스킬이 두 번 들어간다. 폼·스크린샷만 새로 만든다.
    filling              버린다. 절반만 채워진 이력서가 제출되는 것이 이
                         파이프라인 최악의 결과다.

그래서 `filling`만 **시작할 때** 적는다. 끝난 뒤에 적으면 "채우다 죽었다"가
아무 데도 안 남는다.

**재작성(`_revise`)만은 이어받지 않는다(`reuse=False`).** 검증 중에 잡았다 —
이어받으면 수정 지시가 통째로 무시된 채 옛 이력서가 "재작성됨"으로 폰에 다시
올라가고, 사람은 고쳐진 줄 알고 승인한다.

### 곁가지: record_registration이 두 번 정의돼 있었다

앞엣것(913행)은 `fill_report`를 안 쓴다. 지금은 뒤엣것이 살아 있어 문제가
없지만, 순서가 바뀌면 `orchestrator._editor_items()`가 읽는 유일한 신호가
사라진다 — 편집기가 망가져도 자가개선이 아무것도 못 보는 상태로 되돌아간다.

## 전 구간 무인 실주행 시도 — 한도에 막혔고, 막히는 방식이 고장이었다 (2026-08-17)

"자동 계획→승인→fix-run을 사람 손 없이" 한 바퀴 돌리려고 **실제 고장 하나를
재현해 큐에 넣었다.** 고른 고장은 대기 큐에 있던 '텔레그램 400'이다 —
`telegram.notify()`에 4096자를 넘는 텍스트를 주면 400이 나고, `notify()`가
예외를 삼켜서 **그 알림이 그 자리에서 사라진다**(`notify_with_buttons`는
`text[:4096]`, `send_photo`는 `caption[:1024]`로 자르는데 `notify()`에만 그
한도가 없다). 승인 요청이 이 경로로 나가면 사람은 아무것도 못 받는다.

    재현: telegram.notify(conn, "가"*5000) → 400 Bad Request → False 반환
    큐:   error_queue #4 (actionable), 지문 b609e1db49d4

**이 고장은 일부러 안 고쳤다.** 자가복구가 스스로 고칠 첫 실물이다.

기록할 때 알게 된 곁가지: httpx 예외 메시지에 **봇 토큰이 URL로 들어 있다.**
그대로 기록하면 토큰이 error_queue와 계획 프롬프트에 남는다. 이번엔 손으로
가려 넣었지만(`bot<TOKEN>`), `errors.record()`가 자동으로 가리게 하는 것은
아직 안 했다.

### 진짜 수확: 한도가 '가짜 계획'으로 둔갑했다

`cli.py plan`이 자동으로 떴고 `claude`가 exit 1로 죽었다 — **월 지출 한도**다.
그런데 파이프라인은 이렇게 반응했다:

    claude exit 1, stdout "You've hit your monthly spend limit · raise it at ..."
    → _agent()가 returncode를 안 봄 → 그 한 줄이 '에이전트의 답'
    → _parse_plan 실패 → fix_plans #4 "계획을 읽지 못했습니다"(high, 승인대기)
    → hold_for_fix 걸린 채 자동지원까지 정지

한도 하나가 **가짜 계획 한 건 + 파이프라인 정지**가 됐다. 더 나쁜 건
`_trigger_plan`의 빗장이 pending 계획을 세기 때문에, 이 상태에서는 새 고장이
나도 계획이 안 세워진다는 것이다 — 사람이 폰에서 그 가짜 계획을 거절할 때까지.

두 커밋으로 고쳤다:

- **평문 한도를 못 알아봤다** (`llm.py`, 129e809) — f1a8a41이 고친 건 봉투로
  오는 영어 한도(429·session limit)였다. 월 지출 한도는 JSON이 아니라 평문
  한 줄로 와서 `_explain_failure`가 아무것도 못 꺼내고 마커에도 "spend limit"이
  없었다. `raise_if_limited_failure(stdout, stderr)`로 실패 판정을 한곳에
  모았다 — `llm.ask()`·`ask_session()`·`orchestrator._agent`가 같은 claude CLI를
  부르며 판정을 셋으로 나눠 갖고 있어서, 한쪽만 고치면 나머지에서 또 샌다.
- **`_agent`가 exit 코드를 안 봤다** (`orchestrator.py`, d62da9c) — 이제
  exit≠0이면 한도/일반실패를 갈라 예외로 올린다. 그리고 **성공 출력에 대한
  `_raise_if_limited(out)`를 지웠다** — 거기 오는 건 코딩 에이전트의 출력
  전문이라 "rate limit"·"사용 한도"를 *논의하는* 계획문이 그대로 걸린다.
  이 저장소의 계획문은 max_per_day·429를 늘 입에 올리므로 실제 위험이다.
  한도는 exit≠0으로 온다는 게 실측이라 exit 분기가 전부 잡는다.

검증: `_agent`에 가짜 subprocess를 물려 세 갈래(exit1+한도문 → UsageLimited,
exit1+OAuth만료 → RuntimeError, exit0+'한도' 논하는 계획문 → 원문 통과)와,
실제 `python cli.py plan`을 한도 걸린 상태로 돌려
`{"planned":0,"reason":"사용 한도"}` + fix_plans 행 안 생김 + error_queue #4
open 유지 + hold 해제를 DB로 확인했다.

### 남은 것: 한도가 풀려야 전 구간이 돈다

`claude -p`가 월 지출 한도라 **계획·수행 에이전트가 아예 안 뜬다**(대화형
세션은 별개로 돈다). 한도를 올리거나 갱신되면 `python cli.py plan` 한 번으로
error_queue #4에서 계획이 서고, 폰 승인 → `fix-run`까지 이어진다. 그때까지
전 구간 무인 주행은 **미검증**이다.

## 승인 즉시 main 반영 + 자가복구 첫 실주행 (2026-08-17)

`error_queue`가 actionable로 잡은 고장 하나(`claude 실행 실패`)와 사람이
폰으로 신고한 것 하나("승인 눌러도 모르는 버튼")가 계획(#2, #3)까지
세워진 채 승인을 기다리고 있었다. 대화형 세션에서 직접 고쳤다 — 자동
fix-run은 안 거침(코드를 이미 손으로 고친 뒤 fix-run을 돌리면 같은 자리를
두 번 고치게 된다). 자동 계획→자동 승인→자동 fix-run 전 구간을 사람 개입
없이 돌려본 것은 아직 아니다.

- **claude 실패 봉투 500자 컷이 result를 버렸다** (`llm.py`, f1a8a41) —
  exit≠0일 때 원문을 그냥 잘라서, JSON 봉투의 result가 usage 필드들 뒤
  (1200번째 문자 근처)에 있으면 통째로 날아갔다. 원인이 다른 실패 셋의
  fingerprint가 전부 같아지고, 영어 한도 봉투(`api_error_status:429`)는
  한국어 패턴만 보는 `_raise_if_limited`를 피해 그냥 RuntimeError로 샜다
  (1순위 후보였다). `_explain_failure()`로 파싱해 result를 앞으로 꺼내고,
  `_raise_if_limited_payload()`로 **파싱된 필드에서만** 영어 한도 문구를
  본다 — `orchestrator._agent`가 에이전트 출력 전문을 그대로 한도 스캔에
  넘기는 자리(`_raise_if_limited`의 기존 마커 목록)는 절대 안 건드린다.
  계획문에 "한도"라는 단어만 있어도 거기서 오탐되는 걸 재현 확인했다.
- **상주 리스너가 옛 코드를 물고 있었다** (`listener.py`, 32d1413) — 승인
  버튼 콜백 분기가 새 커밋으로 들어온 뒤에도, 그 전에 떠 있던 프로세스는
  구판 그대로 돌아 승인이 "모르는 버튼입니다"로 떨어졌다(sendMessage 없이
  로그도 안 남아 역추적이 오래 걸림). `watch()`가 소스 mtime을 매 drain
  뒤 재서, 바뀌었으면 스스로 return한다 — launchd KeepAlive가 새 코드로
  다시 띄운다. `launchctl kickstart`로 실제 재기동해 확인.

이 둘을 고치다 알게 된 것: **승인 뒤에도 브랜치에만 남기고 사람이 또
`git merge`해야 했다.** 로컬 브랜치일 뿐 GitHub PR도 아닌데, 승인이라는
검토 뒤에 병합이라는 손동작을 또 남긴 셈이었다. `_land()`를 고쳐
(`orchestrator.py`, 3782f68) 사람 승인(`row["status"]=="approved"`)도 low
자동반영과 같은 길로 보낸다 — 단, 결정론 관문(`_auto_gate`)은 승인 여부와
무관하게 무조건 건다(사람은 계획 **문구**를 승인했을 뿐 diff를 본 게
아니라서, 제출·중복·한도 방어선은 그대로 지켜야 한다). 검증 명령은 low
자동반영에는 여전히 필수지만, 승인된 medium/high는 명령이 없어도(계획이
"검증할 방법이 마땅치 않다"고 적었을 수 있다) 막지 않는다 — 승인 자체가
그 자리를 대신한다는 판단이다.

곁가지: `error_queue`의 재계획 방지 빗장(`_trigger_plan`)이 `demoted`
상태를 안 보고 있었다(936cced) — 자동반영이 관문에 걸려 브랜치로 내려온
계획이 있어도 같은 고장이 다시 잡히면 새 계획이 또 뜨는 구조였다.

검증: `_land()`를 git/관문/검증 모두 가짜로 바꿔 7가지 조합(저장소
실측) 확인 — low 자동 3가지(기존 동작 유지), 승인된 medium 3가지(관문
통과+검증없음→main, 관문실패→강등, 검증실패→강등), 미승인 medium(여전히
브랜치 대기) 전부 기대대로. `llm.py`는 429/OAuth만료/모델오류 세 봉투를
재현해 fingerprint가 갈리는 것과 `python cli.py resume 283` 실제 실행으로
회귀 없음 확인. `listener.py`는 `launchctl kickstart`로 재기동해 로그에
"텔레그램 대기 시작"이 새로 찍히는 것 확인. 마무리로 실제 텔레그램에
결과를 알렸고 두 계획의 `fix_plans`/`error_queue`/`control_queue` 행을
`done`/`fixed`로 정리했다.

## 자가복구 — 고장 큐, 계획/수행 분리, 위험도 자동반영 (2026-08-17)

에러가 **사라지고 있었다.** `cli.py` 최상단 핸들러가 "처리 안 된 오류" 한 줄을
폰으로 보내고 끝이라, 그 메시지를 놓치면 그 고장은 어디에도 안 남았다.
`self_items()`가 읽는 신호는 미리 정의한 몇 가지 증상(플랫폼별 지원실패 2건,
fill_report 이상)뿐이라 **처음 보는 고장은 통째로 안 보였다.**

### 분류에 LLM을 안 쓴다 — 그리고 Playwright TimeoutError의 함정

바깥 사정(사이트가 죽음)과 우리 문제를 가르는 데 예외형·메시지 패턴이면
충분하다. 저장소 원칙이기도 하지만 더 실질적인 이유가 있다: **이건 이미 뭔가
고장 난 상태에서 도는 코드다.** LLM 호출은 그 자체가 실패해서(한도·네트워크)
고장을 기록하려다 고장이 난다.

함정은 Playwright `TimeoutError`다. 같은 예외형이 정반대 두 뜻으로 온다:

    waiting for selector   우리 레시피가 화면과 어긋났다   → 고칠 것
    goto / net::ERR_       사이트를 못 열었다             → 바깥 사정

예외형만 보고 external로 넘기면 **이 저장소에서 가장 자주 깨지는 셀렉터 고장이
영원히 큐에 안 들어온다.** 그래서 메시지를 예외형보다 먼저 본다.

### 지문으로 안 묶으면 큐가 무의미해진다

같은 고장이 100번 나도 행 하나에 `count`만 는다. 실측: 같은 `AttributeError`가
공고 283·1025·79에서 각각 다른 경로·id를 달고 왔는데 한 줄로 묶였다(정규화에서
숫자·경로·해시·타임스탬프를 지운다). 안 묶으면 반복 에러가 큐를 도배해서
"쌓인 것을 읽고 계획을 세운다"가 성립하지 않는다. 폰 알림도 같은 이유로 새
지문일 때만 나간다 — 같은 에러에 폰이 100번 울리면 그 알림은 전부 무시된다.

행을 지우지 않고 `status`로 끄는 이유: 같은 지문이 다시 오면 "고쳤는데 또
났다"를 알아야 한다. 그건 처음 난 고장과 **다른 종류의 신호**다.

### 세션을 둘로 가른 이유는 승인 지연이다

새벽 3시에 만든 계획의 승인 버튼은 아침에나 눌린다. 한 세션이 그동안 떠 있을
수 없다. 계획을 `fix_plans`에 굳혀두면 계획 세션은 그 자리에서 끝나고, 승인이
왔을 때 새 프로세스가 그것만 읽고 이어받는다. 덤으로 계획 세션이 코드를 뒤지며
쌓은 컨텍스트(수십 파일)가 수행 세션에 안 딸려온다.

계획에는 Edit/Write를 **주지 않는다.** 계획 단계에서 코드가 바뀌면 그 뒤의
위험도 판정과 검증이 전부 헛것이 된다.

### main에 절대 안 닿는다는 규칙을 뒤집었다

예전 규칙에는 대가가 있었다 — 새벽에 셀렉터가 깨지면 수정이 브랜치에 쌓일 뿐
**다음 사이클은 여전히 깨진 코드로 돈다.** 사람이 아침에 병합할 때까지 그 밤이
통째로 날아간다. 그래서 위험도로 가른다.

**위험도는 에이전트의 자기 채점이다.** 낮게 매기고 틀리는 경우를 막는 건 그
점수가 아니라 실행 결과다. 자동반영에는 조건 셋이 다 필요하다:

    1. 계획이 low라고 말했고
    2. 계획이 적어준 검증 명령이 **실제로 실행돼** 통과했고
    3. `_auto_gate()`의 결정론 검사를 지났을 때

하나라도 걸리면 실패가 아니라 **강등**이다 — 브랜치에 남기고 사람에게 묻는다.

`_auto_gate`가 막는 세 곳은 제출 경로 · 폭주 방어선(`max_per_day/run`) ·
중복 방어선(`canonical_key`)이다. 셋의 공통점은 틀렸을 때 **바깥세상에 되돌릴
수 없는 결과**를 남긴다는 것이다 — 코드 실수는 되돌리면 그만이지만 나간
지원서는 회수가 안 된다. 파일 경로와 diff 내용을 **둘 다** 본다. 경로만 보면
그 코드가 다른 파일로 옮겨왔을 때 뚫린다.

### 검증 명령을 셸에 그냥 넘기면 안 된다

계획이 문자열로 준 명령을 그대로 실행하면 **계획 에이전트가 사실상 임의 실행
권한을 갖는다** — 하네스는 에이전트의 Bash를 막지 이 자리를 막지 않는다.
`cli.py`의 읽기·dry-run 명령만 허용 목록으로 통과시키고 `shell=False`로 돌린다.
`--live`와 셸 문법이 섞이면 실행하지 않는다.

### 하네스는 프롬프트가 아니라 훅이어야 한다

"이런 건 하지 마라"를 시스템 프롬프트에 적는 것은 설득이고, 훅은 차단이다.
`.claude/settings.json`이 아니라 `--settings .claude/agent-guard.json`으로 거는
이유: 프로젝트 설정에 넣으면 **사람의 대화형 세션까지** 같이 묶여서 자기
저장소에서 `git push`를 못 하게 된다.

실측으로 놓쳤다가 고친 규칙: 리다이렉트 차단을 `/(data|profile)/`로 썼더니 앞의
슬래시를 요구해서 `echo x > data/jobs.db`가 그대로 빠져나갔다. 절대경로만
생각하고 쓴 정규식이었다.

### 도는 동안 자동지원을 붙잡는다 — 열쇠를 나눠야 한다

고장 난 채로 지원을 계속 내보내면 고치는 중에도 같은 고장으로 자리가 소모된다.
`FIX_HOLD_KEY`는 사람이 건 `/pause`(`PAUSE_KEY`)와 **다른 열쇠**다. 하나로
합치면 자가복구가 끝나며 푸는 순간 **사람이 걸어둔 정지까지 같이 풀린다** —
사람은 자기가 멈춰둔 줄 아는데 지원이 나가고, 그건 되돌릴 수 없다.

수행이 못 풀고 죽는 경우(kill -9, 재부팅)를 위해 12시간 시한을 뒀다. 그게
없으면 표식만 남아 파이프라인이 영영 멈추고, 사람이 눈치채기까지 며칠이 간다.

### 곁가지로 잡은 것: flush가 버튼을 떨어뜨리고 있었다

`flush-notify`가 사진 없는 알림을 `notify()`로 보내면서 `buttons`를 통째로
버리고 있었다. 지금까지는 지연 알림에 늘 사진이 있어서 안 드러났는데, 새벽에
쌓인 **수정 계획 승인 요청**은 사진이 없다 — 아침에 누를 것 없는 승인 요청이
도착할 뻔했다. `notify_with_buttons()`를 만들어 붙였다.

## 원장은 '우리가 낸 것'만 안다 — 예전에 직접 낸 지원 (2026-08-16)

`apply_ledger`가 중복지원을 막지만, 그건 **이 파이프라인이 낸 것**만 안다.
사용자가 이 프로젝트를 시작하기 전에 직접 낸 지원은 어디에도 없어서 그 자리가
대기열 상위에 그대로 올라온다(실측: 공고 303920 젠스타파트너스, 88점으로
actionable). 예전 코드라면 이력서 조립(LLM 3~4회)과 사본 등록까지 다 한 뒤에야
지원 폼에서 막혔다 — 돈과 시간을 다 쓰고 나서다.

**작업을 시작하기 전에 공고 페이지를 한 번 연다**(`apply.preflight`, 11초).
판정은 세 갈래고, 양쪽 다 **긍정 신호**로 본다:

    이미 지원  button '지원완료'(disabled)가 있다
    미지원     apply__ 패널이 있다 (apply__done__net '제출하기')
    모름       둘 다 없다 → 막지 않고 진행한다

'지원하기 버튼이 없으면 지원한 것'으로 읽으면 안 된다. 지원 패널이 이미 열려
있는 공고에는 '지원하기'가 아예 없다(실측: 공고 374503 인졀미 — 미지원인데
'지원하기' 0개). 그리고 모르는 것을 '지원함'으로 단정하면 멀쩡한 자리를 조용히
버린다 — 반대 방향(모르는데 진행)은 지원 폼에서 걸리므로 손해가 시간뿐이다.

한 번 확인하면 원장에 `status='external'`로 적어 **다시는 올라오지 않게** 한다.
매번 확인하면 옛 지원이 쌓일수록 확인만 하다 끝난다. 대기열이 이미 "원장에 있는
canonical_key는 뺀다"는 규칙을 쓰므로, 같은 공고가 다른 플랫폼에 중복 게시돼
있어도 같이 걸린다.

`submitted`가 아니라 `external`인 이유: 우리가 낸 게 아니다. 제출 건수·성공률에
섞이면 "이 파이프라인이 무엇을 했나"를 못 읽는다. 일일 상한도 이 상태는 안 센다
— 오늘 발견했다는 이유로 오늘 낼 수 있는 자리가 줄면 안 된다.

### 다음 수집부터는 상세도 안 받는다

"수집을 아예 안 할 수 있나"의 답은 **반은 된다**이다. 목록 API에는 "이건 빼고
줘"가 없어서 목록에 실려 오는 것 자체는 못 막는다. 대신 실제 비용인 **상세
조회를 건너뛴다** — 공고 하나당 요청 하나이고 수집 시간의 거의 전부다. 게다가
`detail_limit`(800) 자리를 하나 차지하므로, 다시 낼 일 없는 공고가 **진짜
후보를 밀어낸다.** canonical_key는 목록 데이터(회사명+제목)만으로 만들어지므로
받기 전에 거를 수 있다(실측: 745건 통과 중 3건 생략).

**키 하나로는 부족하다.** canonical_key는 회사명+제목의 해시고, 원장의 키는
적을 때 값으로 굳어 있다. 반면 수집은 매번 새 제목으로 키를 다시 계산한다 —
회사가 제목을 고치면 이미 지원한 자리가 '처음 보는 자리'로 돌아온다. 실측으로
그 자리에 걸렸다: 공고 4110의 제목을 뒤늦게 정정했더니 키가 갈려 상세 조회가
다시 나갔다. 그래서 (플랫폼, 공고 id)로도 본다 — 그건 제목이 바뀌어도 안 변한다.
같은 구멍이 대기열 뷰에도 있어서 `v_actionable`에 `job_id` 차단을 같이 넣었다.

정규화가 세서 웬만한 제목 변형은 같은 키로 흡수된다("Back-end Engineer(신입)"
→ "Back-end Engineer (신입/경력)"은 같은 키). 갈리는 건 언어가 바뀌는 수준의
변형이다("백엔드 엔지니어", "서버 개발자(신입)") — 그때 공고 id가 마지막
방어선이 된다.

## 상주 브라우저를 공유하기 시작하면서 생긴 사고 3종 (2026-08-16)

창 누수를 고치려고 "한 번 띄우고 계속 붙는다"로 바꾼 뒤(d1848d6), 브라우저가
**공유 자원**이 됐다. 그런데 이 저장소는 긴 작업을 전부 `subprocess.Popen`으로
fire-and-forget 한다 — 서로의 존재를 아무도 모르는 채 하나의 창을 나눠 쓰는
구조가 됐고, 아래 셋이 한꺼번에 터졌다. **공유 자원으로 바꿀 때는 "누가
쓰는가"와 "누가 소유하는가"를 같이 정해야 한다**는 게 교훈이다.

**1. 포트는 신원이 아니다.** `_attach()`가 CDP 9222가 응답하면 그냥 붙었다.
9222는 흔한 기본값이라 다른 크롬이 얼마든 같은 포트를 잡는다 — 사람이 클로드
재로그인(OAuth)하려고 띄운 크롬에서 우리 작업이 돌았다. 더 나빴던 건
`_kill_resident()`로, 포트 점유자를 무조건 kill 했다(사람 브라우저를 죽이는
코드다). 이제 lsof로 pid를 찾아 `ps`의 명령줄에서 `--user-data-dir=<BROWSER_DIR>`을
확인한다(`resident_owner()`). 프로필이 곧 로그인 세션이 사는 곳이라, 프로필이
같다는 건 "우리 세션이 있는 창"과 같은 말이다.

**2. `pages[0]`은 우리 탭이 아니다.** 그냥 "첫 번째 탭"이다. 사람이 그 창에
탭 하나를 열면 자동화가 그걸 몰고 다닌다. 만든 탭의 CDP targetId를
`.browser_tab`에 적어두고 그 탭만 쓴다. URL·제목으로 고르면 안 된다 — 사람이
원티드를 직접 열어두는 일이 잦다. (실측: 우리 탭을 닫고 사람 탭만 남긴
상태에서 붙여보면, 예전 코드가 사람 탭을 몰았을 자리에서 새 탭을 연다.)

**3. 뒤에 온 작업이 앞의 탭을 뺏는다.** 수집 중에 폰에서 지원 버튼을 누르면
뒤에 온 쪽이 같은 탭을 다른 URL로 몰고 갔고, 앞 작업은 셀렉터를 기다리다
타임아웃으로 죽었다. 남는 메시지가 "요소를 못 찾음"이라 **진짜 원인이 어디에도
안 남는다** — 이 종류의 실패는 로그만 봐서는 영원히 못 찾는다. flock 파일
잠금으로 직렬화한다(`runner/lock.py`). 파이썬 안의 락으로는 안 된다 —
경쟁하는 쪽이 스레드가 아니라 별개 프로세스다.

### 곁가지로 드러난 것: 부분 수집이 본문을 지웠다

중단 기능을 검증하다 발견했다. 저장 루프는 상세를 받았든 못 받았든 전체를
훑는데, 못 받은 공고는 `description=''`로 와서 **예전에 받아둔 본문을
덮어썼다.** 22건에서 멈춘 수집 한 번에 actionable이 16 → 3으로 주저앉았다.
`detail_limit`(800)을 넘긴 공고에는 늘 일어나던 일이고, 중단 기능이 그걸 크게
만들어 드러냈을 뿐이다. `upsert_job`이 빈 본문으로는 덮어쓰지 않게 고쳤다.

같은 이유로 **중단된 수집으로는 건강을 판정하지 않는다.** 상세를 덜 받았으니
NO_DETAIL이 통과분을 지배하는 게 당연한데, 경보는 "망가졌다"고 폰에 울렸다.

### /stop이 실제로 듣는 곳 (그리고 안 듣는 곳)

협조적 중단은 **표시를 보는 루프에서만** 듣는다. 지금 보는 곳은 셋이다:

    수집        공고 한 건 사이 / 상세 조회 한 건 사이
    night-cycle 준비 한 건 사이
    (그 외)     안 본다 → `/stop 강제`가 유일한 방법

자기개선(`improve`)이 대표적으로 안 듣는 쪽이다. 안에서 `claude` CLI가
수 분씩 블로킹하고 기본 limit=1이라 "건 사이"라는 게 사실상 없다. 그래서
강제 종료는 프로세스 그룹째 보낸다 — 부모만 죽이면 `claude`가 고아로 남아
"멈췄다"고 답한 뒤에도 일이 계속 진행된다.

**대상을 골라 멈출 수 있어야 한다.** 처음엔 전부 멈추는 것 하나뿐이었는데,
검증 중에 지원준비를 접으려다 같이 돌던 수집을 두 번 날렸다(둘 다 상세
조회 중이라 수십 분어치였다). 지금은 `/stop 수집`, `/stop 12`로 고른다.

## 'AI 활용 경험'은 사람 몫이다 — 자동화가 안 건드린다 (2026-08-16)

원티드의 'AI 활용 경험' 칸을 **사용자가 사본메이커에 직접 등록해 뒀다.** 모든
이력서는 그 사본에서 복제되므로 이미 채워진 채로 온다. 그런데 우리 파이프라인은
공고마다 이 칸을 새로 써서 덮어쓰고 있었다 — 50자 한 줄 칸이라 매번 문장이
바뀌고, 사람이 확정한 문구가 사라졌다.

학력·링크를 사본에 맡긴 것과 같은 판단이다(`COPY_STEPS`): **공고에 따라 달라지지
않는 값은 다시 쓰지 않는다.** 다시 쓰는 것 자체가 위험만 늘린다.

- `assemble.py` — 편집기 스키마에서 `ai_usage`를 뺐고, 모델이 가이드 §6-1을
  읽고 되살려 넣어도 `_drop_manual_fields`가 지운다(프롬프트로만 막으면 샌다).
  50자 클램프(`_clamp_fields`/`FIELD_LIMITS`)는 쓸 데가 없어져 지웠다.
- `resume_editor.py` — 채우는 루프에서 뺐다(`MANUAL`). 셀렉터는 남겼다:
  칸이 사라졌는지 확인하고 자가수복이 고칠 수 있어야 한다.
- `resume-guide.md` §6-1 — "작성하지 않는다"로 다시 썼다. 근거 표는 남긴다.
  그 수치들은 간단 소개·주요 성과에 녹여 쓰는 사실 저장소다.

캐시된 조립 결과(`profile/generated/*.json`)에는 `ai_usage`가 남아 있지만
채우는 쪽이 읽지 않으므로 지우지 않았다.

## 이력서 원본은 편집도 금지다 (2026-08-16)

무인 준비가 `preview_resume_url`에 저장된 이력서 하나를 계속 재사용했다.
이력서가 계정에 쌓이는 걸 막으려던 것인데, 세 가지가 같이 깨졌다:

- `resume_url`을 넘기면 `_autoapply`가 `template=None`으로 바꿔 넘겨
  **`pick_template`이 통째로 무시된다.** 트랙별 사본메이커를 만들어 둔 의미가
  없었고, 무인 사이클은 그 분기를 한 번도 안 탔다.
- 재사용 대상이 '박예일 기본'으로 굳어 있었다. 스킬은 "사본은 비어 있다"를
  전제로 **추가만** 하므로(prune 없음), 같은 이력서를 공고마다 덮어쓰면 스킬이
  계속 쌓인다.
- 그 상태로 공고 33에 **실제 제출까지 나갔다**(apply_ledger id=4,
  resume_title='박예일 기본').

재사용을 지웠다. 그리고 `resumes.protect`를 **삭제 금지에서 편집 금지까지**로
넓혔다 — `fill()`이 보호 제목을 열면 `ProtectedResume`로 그 자리에서 멈추고,
`_submit`은 보호 이력서가 등록된 공고를 제출하지 않는다. 편집기는 자동저장이라
한 필드만 지나가도 되돌릴 수 없고, 사본메이커가 오염되면 거기서 뜬 사본이 전부
같이 오염된다. 아껴지는 이력서 몇 개와 바꿀 값이 아니다.

쌓이는 이력서는 `resumes --cleanup`이 치운다. 거기에 **사라진 이력서의 기록을
지우는 일**도 붙였다 — 사람이 플랫폼에서 직접 지우면 `made_resumes`에만 남아
"아직 있나?" 하고 헷갈리게 된다. `resume_builds`는 안 건드린다. 거기는 무엇을
만들어 무엇을 냈는지의 이력이고, 이력서가 사라졌다고 그 사실이 사라지진 않는다.

## 상주 리스너가 run.sh를 안 거치면 PATH에 claude가 없다 (2026-08-16)

`/guide` 요청이 "시작합니다" 확인만 오고 diff가 안 왔다. 원인 추적이 어려웠던
이유는 `com.autoapply.listen`이 `subprocess.Popen(stdout=DEVNULL,
stderr=STDOUT)`으로 `cli.py`를 fire-and-forget 실행하는데, 안에서 죽어도
그 출력이 전부 버려져서 아무 데도 안 남았다는 것이다.

`cli.py` 최상단에 잡히지 않은 예외를 텔레그램으로도 보내는 안전망을 먼저
넣고 나서야("cli.py 처리 안 된 오류") `ClaudeUnavailable: claude CLI를 찾을
수 없습니다`가 드러났다.

진짜 원인: `com.autoapply.night`/`flush`는 `run.sh`를 거치는데, `run.sh`가
`export PATH="$HOME/.local/bin:..."`를 스스로 하고 있었다. 반면
`com.autoapply.listen`은 `run.sh`를 안 거치고 launchd가 `python`을 직접
띄운다 — launchd 기본 PATH(`/usr/bin:/bin:/usr/sbin:/sbin`)엔 `claude`가
없고, 리스너가 띄우는 모든 서브프로세스(`/guide`, `/지원시작`, `/improve`,
수정요청 재작성)가 그 PATH를 그대로 물려받는다.

`com.autoapply.listen.plist`에 `EnvironmentVariables/PATH`를 넣어
`run.sh`와 같은 값으로 맞췄다. 검증은 리스너 프로세스의 실제 환경변수를
그대로 복제해 `cli.py guide`를 돌려 끝까지 통과하는 걸 확인했다(PATH 없이
`env -i`로 돌리면 바로 `ClaudeUnavailable`로 죽는 것도 대조 확인).

**교훈**: launchd 잡을 새로 추가할 때 `run.sh`를 거치지 않는 잡은 PATH를
따로 챙겨야 한다. 하나가 되면 나머지도 된다고 가정하면 안 된다.

## 이력서 정리는 '우리가 만든 것'만 지운다 (2026-08-16)

`resumes --cleanup`이 사람이 직접 만든 이력서를 지웠다
(`[포지션 리뷰] 코르카 Software Engineer`). 지원 기록에도 없고 로컬 사본도
없어서 되살릴 수 없었다.

원인은 기준의 순서였다. '오래되었거나 개수를 넘은 것'으로 고르면 **누가
만들었는지를 묻지 않는다.** 나이와 개수는 무엇을 지울지 정하는 기준이 아니라,
**우리 것 중에서 무엇부터 지울지** 정하는 기준이어야 했다.

이제 `made_resumes` 표가 소유의 근거다. `prepare_from_template`이 이력서를
만들자마자 적는다 — 채우다 실패한 것도 우리가 치울 물건이라 같이 적는다.
`resume_builds`로는 안 된다. 거기는 공고당 한 줄만 남아서(upsert) 같은 공고를
여러 번 만들면 예전 것이 기록에서 사라지고, 그러면 우리가 만든 이력서인데도
'모르는 것'이 되어 영영 안 지워진다.

# 남은 작업 큐

## 이력서는 사본에서 시작한다 (2026-08-16 도입)

`resumes.copy_from`이 트랙·키워드로 사본메이커를 고르고, 목록 ⋯ 메뉴의
'사본 만들기 → 이력서 제목 변경'으로 공고별 이력서를 만든다.

이걸로 한꺼번에 사라진 문제들:

- **완성도 71% 정체** — 학력(입력돼도 저장 안 됨)과 링크(입력칸 TimeoutError)가
  원인이었다. 둘 다 공고와 무관하게 늘 같은 값이라, 사본이 들고 오면 그만이다.
- **중복 제목** — '박예일 14'가 둘이라 지원 폼에서 무엇을 고르는지 알 수 없었다.
  이제 '{회사} {공고제목} #{n}'으로 붙이고, n은 DB 카운터(settings.resume_seq)에서
  받는다. 목록을 세지 않으므로 화면 파싱이 틀려도 번호가 겹치지 않는다.
- **스킬 정리(prune) 위험** — 사본 스킬은 비어 있어 추가만 한다.

검증(공고 79, 코드비전): 저장안됨 0, 플랫폼 미입력 0, 날짜 10/10, 스킬 12/12,
작성 완료, 학력·링크·언어 보존.

폴백은 남겨뒀다. 사본메이커가 계정에서 사라지면 빈 이력서 경로(`ALL_STEPS`)로
돌아간다 — 느리고 71%에서 멈출 수 있지만, 파이프라인이 서는 것보다는 낫다.

날짜는 갈라 다룬다. **재직기간**은 사본 값을 그대로 두고(회사에 다닌 기간은
공고에 따라 안 바뀐다), **성과 기간**은 매번 설정한다(성과는 공고마다 다시 쓴다).

### 확인했으나 문제가 아니었던 것
- '박예일 영업 사본메이커'가 **작성 중**인 것 — 사람이 '작성 완료'를 안 누른
  상태일 뿐이다. 완성도 패널의 미체크는 '글자수 400자 이상' 하나뿐이었고,
  우리 파이프라인은 간단 소개를 450자 이상으로 채운 뒤 finalize()를 부른다.
  비전이 '시험명* 미입력'이라고 보고했지만 그건 회색 템플릿 자리였다 —
  `vision.verify()`가 막아둔 오탐이 `ask()`에는 안 걸려 있었다(지금은 걸린다).
- **공고 9(트리플오스) `RecipeError: #name 요소를 찾지 못함`** — 원티드가
  공고/회사별로 지원 폼 레이아웃을 다르게 내려주는 게 아니었다. 같은 공고로
  `goto→click(지원하기)→expect(#name)` 체인을 5번 재현(autoapply 전체 1회 +
  체인만 4회)했는데 5/5 모두 #name을 정상적으로 찾았다. 대신 같은 재현에서
  **스크린샷 저장이 매번(지금까지 7/7) `waiting for fonts to load` 15~30초
  타임아웃으로 실패**했다 — 원래 사건의 스크린샷 실패도 같은 메시지였다.
  `session.py`가 브라우저 창을 화면 밖에 숨겨 띄우는 구조라(`hidden` 기본값)
  렌더링이 간헐적으로 느려지는 것으로 보인다(같은 조건에서도 소요시간이
  23초~67초로 널뛰었다). `recipes/wanted.json`의 `#name` expect timeout을
  15000→30000으로 올려 여유를 줬다(검증: 3회 추가 재현 모두 통과).
  **스크린샷 타임아웃은 미해결로 남긴다** — `session.py`의 `screenshot()`에
  `timeout=30000`을 줘봤지만 3/3 그래도 실패해서(원인이 폰트 로딩만이 아니라
  풀페이지 합성 자체가 느린 것일 수 있다) 되돌렸다. 아래 대기 큐 참고.

### 남은 것
- 경력 **삭제**는 아직 못 한다. 지금은 수정·추가만 한다. 사본에 담긴 경력이
  공고와 안 맞을 때 지우려면 행 삭제(🗑) 조작이 필요하다.
- biz/presales 트랙으로 실제 한 바퀴를 안 돌려봤다. 지금까지 검증은 dev뿐이다.
- ~~`resumes --cleanup` 기준을 실제로 돌려봐야 한다~~ — 2026-08-16 실행함
  (max_keep 31 / max_age_days 14). 지울 후보 0건, 우리 것 아닌 포트폴리오
  4건은 건너뜀, 사라진 이력서 기록 8건 정리. 사본메이커와 '박예일 기본'은
  `protect`에 있고, 그 목록은 이제 **편집 금지**이기도 하다.


자기개선 루프가 읽는 목록. **맨 위 미완료 항목 하나만** 하고, 끝나면 여기서
체크하고 커밋한다. 한 번에 여러 개를 하지 않는다 — 검토가 불가능해진다.

새 항목은 아래 규칙으로 넣는다:
- 무엇을 검증하면 끝난 것인지(`검증:`)를 반드시 적는다. 없으면 끝을 알 수 없다.
- 되돌릴 수 없는 동작(실제 지원 제출)은 이 큐에 넣지 않는다. 사람이 한다.

---

## 진행 중

(비어 있음)

## 대기

- [ ] **`claude setup-token`으로 로그인 만료 없애기** (사람이 할 일)
  브라우저 승인이 필요해 자동화가 못 한다. `claude setup-token`을 실행해
  나온 1년짜리 토큰을 `schedule/com.autoapply.*.plist`의
  `EnvironmentVariables/CLAUDE_CODE_OAUTH_TOKEN`에 넣고 `launchctl` 재적재.
  이 토큰은 `/login` 자격증명보다 우선하고 모델 호출만 가능하다(이 저장소
  용도엔 충분). 안 하면 만료 때마다 새벽 사이클이 통째로 선다.
  검증: `LoginExpired` 알림이 더는 안 온다.

- [ ] **검수·조립 프롬프트의 프리픽스를 맞춰 캐시를 적중시킨다**
  실측: 검수 한 번에 입력 30,643토큰 중 22,757이 **캐시 쓰기**였다(가이드 33KB).
  조립과 검수가 같은 가이드를 쓰는데 프롬프트 앞부분이 달라 캐시가 안 맞는다.
  가이드·공고를 프롬프트 맨 앞 고정 블록으로 올리고 지시문을 뒤로 내리면 두 번째
  호출부터 캐시 읽기로 처리될 수 있다.
  검증: 같은 공고를 연속 조립할 때 `llm-cost`의 `캐시읽기`가 늘고 `캐시쓰기`가 줄며
  `비용_usd` 합이 내려감.

- [ ] **텔레그램 400** — 원인 하나는 잡았다: `notify()`에만 길이 제한이 없어
  4096자를 넘기면 400이고, 예외를 삼켜서 알림이 조용히 사라진다(재현 확인).
  `_call()`이 400을 `httpx.HTTPError`로 보고 2번 더 재시도하는 것도 무의미하다
  — 영구 실패다. **error_queue #4로 큐에 넣어 뒀고 손으로 안 고친다** —
  자가복구가 스스로 고칠 첫 실물이다(위 2026-08-17 항목 참고).
  링크 캡션에서 난 400은 별개일 수 있다(HTML 앵커 파싱). 그건 아직 미확인.
  검증: 4096자 초과 알림이 잘려서라도 전송됨 + 링크 포함 캡션 정상 전송.

- [ ] **고장 기록에 봇 토큰이 섞인다** — httpx 예외 메시지가 API URL을 통째로
  담아서 `bot<토큰>/sendMessage`가 error_queue와 계획 프롬프트에 남는다.
  `errors.record()`가 저장 전에 가려야 한다.
  검증: 토큰이 든 예외를 기록했을 때 message·traceback에 토큰이 없음.

- [ ] **사람인·자소설 편집기 레시피**
  이제 셀렉터가 `recipes/<platform>.json`의 `editor` 섹션에 있으므로, 다른
  플랫폼은 파이썬을 고치지 않고 레시피 파일만 추가하면 된다.
  동작 방식(자동완성·날짜피커·호버추가)이 원티드와 다르면 그때 코드를 본다.
  검증: 사람인 이력서 편집기에 dry-run으로 필드 존재 확인.

- [ ] **레시피 빌더에 비전 연결**
  `vision.ask()`가 준비됐다("이 화면에서 제출 버튼의 라벨과 상태는?" 같은 질의가
  실제로 동작). 레시피가 깨졌을 때 화면을 읽고 셀렉터를 다시 만드는 루프에 쓴다.
  검증: 일부러 깨뜨린 레시피를 비전 단서로 고쳐 dry-run 통과.

비전 점검이 찾은 것들 (DOM 대조는 전부 놓쳤다):

- [ ] **링크 2개 이상**
  행 호버 → solid 아이콘으로 시도했으나 행이 안 늘어난다(1개에서 멈춤).
  덮어쓰기는 막았다. 학력과 같은 방식인데 왜 안 되는지 확인 필요.
  검증: GitHub·포트폴리오 둘 다 남음.

- [ ] **스킬 정리(prune)가 켜면 스킬을 없앤다** — 기본 꺼둠
  삭제 자체는 되고 새로고침 후에도 유지된다(칩 안 svg 클릭). 그런데 삭제 뒤에
  추가한 것이 저장되지 않아 11개 → 1개가 됐다. 누적보다 손실이 나쁘므로
  `skill.prune: "on"` 을 레시피에 넣어야만 동작하게 막아뒀다.
  다음에 볼 것: 삭제 후 추가 경로에서 저장 요청이 나가는지(날짜 칸과 같은 부류로
  같은 섹션의 다른 칸을 건드려야 PATCH가 나가는 구조일 수 있다).
  검증: 정리를 켠 상태에서 화면 스킬이 JSON과 일치.

  참고: 정리를 꺼도 실측상 큰 문제는 없었다. 미리보기 이력서를 덮어 쓰면
  이전 스킬이 대체로 밀려나고, 남는 것은 목표와 겹치는 항목이었다.

- [ ] **스킬 이름 매칭 보강**
  `REST API`·`GitLab CI/CD`·`AWS S3`가 원티드 DB에 없어 빠진다. 공백·슬래시가
  들어간 이름을 어떻게 매칭할지 정해야 한다.
  검증: `skills_skipped`가 비거나, 빠진 것이 화면 목록과 일치.


- [ ] **이미 쌓인 이력서 정리** (사람이 할 일)
  `박예일 2~6`은 안 쓰인다. `박예일 7`이 미리보기용이므로 남긴다.
  **삭제는 이 도구가 하지 않는다** — 계정 데이터를 지우는 건 되돌릴 수 없고,
  목록에 삭제 버튼도 없어 항목별 메뉴를 눌러야 한다. `cli.py resumes`로 목록을
  확인하고 원티드에서 직접 지울 것.

- [ ] **경력 2건 이상**
  학력은 행 호버 → 행 안쪽 solid 아이콘으로 해결됐다. 경력도 같은 방식일 것으로
  보이나 아직 확인 안 했다. 지금 경력이 1건뿐이라 검증할 데이터가 없다.
  검증: 경력 2건인 JSON으로 채웠을 때 둘 다 저장됨.

## 완료

체크박스 단위 이력은 git 로그와 커밋 메시지에 있다(CLAUDE.md 규칙상 커밋
메시지가 "왜"를 담는다). 여기 최근 항목만 남기고, 오래된 것은 지운다 —
같은 내용이 위 날짜별 항목이나 [TODO.md](TODO.md) 완료 목록에 이미 있다.

- [x] 편집기 JSON 경로에 검수 연결 — 스킬은 코드 대조, 산문은 Sonnet 5.
      차단하지 않고 승인 화면에 띄운다 (2026-08-17, 위 참고)
- [x] 이력서 강조 순서를 직무명 표에서 **공고 자격요건**으로 옮김 + 간단 소개
      대표 프로젝트 링크(§8) + 이력서로 못 하는 요구는 폰 승인 화면에만
      (2026-08-17, 위 참고)
- [x] 자가복구 첫 실주행 — `error_queue`가 잡은 고장 2건을 대화형 세션에서
      직접 고침 + 승인 즉시 main 반영으로 흐름 변경 (2026-08-17, 위 참고)
- [x] 고장 큐 · 계획/수행 분리 · 위험도 자동반영 (2026-08-17, 위 참고)
- [x] 예전에 직접 낸 지원을 작업 전에 잡기 (2026-08-16, 위 참고)
- [x] 상주 브라우저 공유 사고 3종 정리 (2026-08-16, 위 참고)
- [x] 'AI 활용 경험'·이력서 원본 자동화 제외 (2026-08-16, 위 참고)
- [x] 이력서는 사본에서 시작 — 완성도 71% 정체 해소 (2026-08-16, 위 참고)
- [x] 텔레그램 "/" 자동완성 등록 + `/apply` 통일, 답장 재시도, 서비스워커
      정리, 증적 스크린샷 자동 회복, 포트폴리오 매칭 — 전부 2026-08-16,
      상세는 커밋 로그(`git log --oneline`)에서 날짜로 찾을 것
- [x] 체인 최초 완주 (수집→판정→작성→등록→승인→제출) — 공고 1025,
      2026-08-16
