# 남은 작업 큐

자기개선 루프가 읽는 목록. **맨 위 미완료 항목 하나만** 하고, 끝나면 여기서
체크하고 커밋한다. 한 번에 여러 개를 하지 않는다 — 검토가 불가능해진다.

새 항목은 아래 규칙으로 넣는다:
- 무엇을 검증하면 끝난 것인지(`검증:`)를 반드시 적는다. 없으면 끝을 알 수 없다.
- 되돌릴 수 없는 동작(실제 지원 제출)은 이 큐에 넣지 않는다. 사람이 한다.

---

## 진행 중

(비어 있음)

## 대기

- [ ] **학력 종료일이 되돌아간다**
  6개 날짜 중 학력종료만 새로고침하면 `2026.08`(오늘 날짜)로 돌아간다.
  나머지 5개는 정상. 시도한 것: 연·월 선택 후 `확인` 클릭(다른 칸은 이걸로 해결),
  선택 직후 화면 값 대조(그 시점엔 2024.12로 보인다) — 즉 UI에는 반영되나
  저장이 안 되고 오늘 날짜로 덮인다.
  다음에 볼 것: 졸업 상태 값과의 연동 여부(재학중이면 종료일을 오늘로 강제하는지),
  저장 요청의 네트워크 페이로드.
  검증: 새로고침 후 학력종료가 조립 JSON의 `educations[0].end`와 일치.

- [ ] **경력·학력 여러 건**
  지금은 첫 칸만 채운다. '주요 성과 추가' 버튼을 눌러 칸을 만들어야 한다.
  실패 시 빈 칸이 남으므로 실패하면 만든 칸을 되돌릴 것.
  검증: 경력 2건 이상인 JSON으로 채웠을 때 모두 저장됨.

- [ ] **`resume → editor → apply` 한 명령으로 연결**
  지금은 세 단계가 따로다. `cli.py autoapply <job_id>` 하나로 잇는다.
  실제 제출은 `--live` 없이는 하지 않는다.
  검증: dry-run으로 이력서 생성 → 편집기 반영 → 지원 폼 채움까지 한 번에 통과.

- [ ] **`_job()`이 config의 이력서 제목을 요구하는 문제**
  이제 이력서를 매번 만들므로 `applicability.resumes` 매핑이 필요 없다.
  방금 만든 이력서 제목을 레시피에 넘기도록 바꾼다.
  검증: `resumes.wanted.dev`가 null이어도 apply dry-run이 통과.

- [ ] **`gaps` 되먹임을 판정에 반영**
  어셈블러가 낸 필수 미충족 항목이 지금은 이력서 조립 단계에서만 쓰인다.
  `applicability`에 blocker로 넣어 `v_actionable`에서 빠지게 한다.
  검증: 필수 gap 3개인 공고가 `cli.py targets`에 안 나옴.

- [ ] **스킬 후보 없는 항목 처리**
  원티드 스킬 DB에 없는 이름은 조용히 건너뛴다. 몇 개가 빠졌는지 보고하고,
  많이 빠지면 가이드의 스킬 표기를 원티드 표기에 맞추도록 알린다.
  검증: 건너뛴 스킬 목록이 결과에 포함됨.

## 완료

- [x] 지원 속도 상한 (`claim()`에서 일일/실행 단위 차단)
- [x] Playwright 러너 + dry-run 기본값
- [x] `recipes/wanted.json` — 실제 제출로 검증
- [x] 세션 생사 자동 감지 (URL 리다이렉트 기준)
- [x] L1 이상 감지 (LLM 0회)
- [x] 파이프라인 A/B 분리 + launchd 2시간 주기
- [x] 이력서 어셈블러 (작성 → 검수 → 보강)
- [x] 편집기 자동 입력 — 완성도 100%
- [x] 스킬 자동 등록
- [x] **주요 성과 기간** — 스키마에 achievements.start/end 추가.
      실측: 성과 2024.01–2024.02가 재직기간 2023.08–2024.04 안에 정상 배치.
      날짜 6개 중 5개 검증 통과 (학력종료는 별도 항목으로 분리).
- [x] **링크 칸 자동 입력** — 3개 등록, 완성도 100% 확인.
      안내 문구로 여는 방식은 부분 입력 후 문구가 바뀌어 깨진다.
      해시가 아닌 `.link-view` 클래스로 행을 잡는다.
- [x] **회사명 자동완성 글자 유실** — 검증 통과 (회사명·학교명·전공 3/3 원본 일치).
      후보가 없을 때 Tab으로 확정되고, 값이 다르면 재입력하는 교정 경로가 잡아준다.
      잘리는 위치가 매번 달라 재시도만으로는 못 고치므로 **대조 검증이 본체**다.
