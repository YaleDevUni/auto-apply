# CLAUDE.md

autoapply 저장소에서 작업할 때 지키는 세 가지. 어긴 걸 알아챘으면 그 자리에서
바로잡고 계속한다 — 다음 세션에 넘기지 않는다.

## 1. 개발 후 검증 필수

"됐을 것"이라고 말하지 않는다. 실제로 실행해서 확인한다.

- 브라우저 조작이면 **새로고침 후 값을 다시 읽는다** — 화면에 보여도 저장 안
  되고 사라지는 경우가 이 저장소에서 잡은 버그의 대부분이었다.
- CLI·함수 변경이면 실제로 돌려서 출력을 본다. `python cli.py <cmd>` 실행 없이
  "이렇게 하면 될 것"으로 끝내지 않는다.
- 검증이 막히면(브라우저 세션이 없다 등) 막혔다고 명시한다. 확인 불가와
  성공을 같은 것으로 보고하지 않는다.

## 2. 기능 단위로 커밋한다

한 커밋 = 한 가지 이유. 여러 기능을 한 커밋에 묶으면 나중에 뭐가 뭘
깨뜨렸는지 못 가린다.

커밋 메시지는 **무엇을 했는지가 아니라 왜 그렇게 했는지**를 적는다 — 특히
실측으로 알아낸 함정(어떤 셀렉터가 왜 안 먹었는지 등)은 반드시 남긴다. 다음에
같은 데서 또 막힌다.

## 3. TODO.md / NEXT.md — 컨텍스트를 확보하고 갱신한다

작업 시작 전에 [TODO.md](TODO.md)(전체 지도)와 [NEXT.md](NEXT.md)(최근 변경 +
남은 큐)를 읽는다. 이 저장소는 세션 사이에 기억이 없다 — 여기 안 적힌 결정은
다음 세션에서 그대로 다시 논의된다.

- **방향이 바뀌면 그 자리에서 갱신한다.** 나중에 몰아서 하지 않는다 — 그 사이
  다른 세션이 옛 방향을 기준으로 작업할 수 있다.
- **끝난 항목은 완료로 표시하거나 지운다.** 큐에 끝난 일이 남아 있으면 다음
  세션이 이미 한 일을 또 한다.

세부 구현 원칙(LLM은 이력서 작성/검수에만, 되돌릴 수 없는 동작 금지 등)은
[.claude/skills/continue-autoapply/SKILL.md](.claude/skills/continue-autoapply/SKILL.md)에
있다.
